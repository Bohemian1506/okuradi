# Day 5（2026-09-21）始めるコマンドとリハーサル手順。番組側の決定を2つ受け取り、その日のうちに実装した

**PR 6件。コード・道具・テストが 250行（新しいファイル2つ）、文書が 399行**
（`docs/dev-log/day-stats.sh` で測った）。**`pytest` は 246件。**

午前は `/始め`（#124）。午後は **#53 のリハーサル手順を書き**（#126）、
**番組側（r-hoso）から決定が2つ届いた**（#127 と #106 のコメント）。

夜に **#106 を実装した**（#129）。**この日はじめて `build.py` を触った。**
続けて、**day-1〜day-5 の議事録から失敗パターンを数え、守りを3つ足した**（#130）。

> **この冒頭は、区切るたびに測り直して書き直す。**
> 夕方の版は「PR 3件。**実装は触っていない**」だった。夜に `build.py` と `tests/` を触ったので、
> そのままなら嘘になっていた。day-4 と同じ形（`docs/dev-log/README.md` の「冒頭のまとめ」）。

## #124 feat: 作業を始めるときのコマンド /始め を足す

### きっかけ

「作業開始時に申し送り・メモリー・GitHub の Issue を確認するコマンドはあるか」と聞かれた。
手元を全部見たら、**開始側のコマンドは weight_dialy の `/mode` だけ**で、okuradi にはなかった。
okuradi にあるのは `/区切り`（終了側）だけ。**書き手はいるのに、読み手がいない状態だった。**

### 検討したこと

**まず、3つのうちコマンドが要るのは1つだけだと分かった。**

| 見るもの | コマンドが要るか |
|---|---|
| メモリー | 要らない。毎セッション自動で読まれる |
| Issue / PR / 手元の状態 | 要らない。`.claude/hooks/session-start-status.py` がもう出している |
| **申し送り** | **要る。読んで、実物と突き合わせる手順がなかった** |

**置き場所で3案を出した。**

- **A. okuradi に1つ作る** — `/区切り` と対になる。申し送りの場所が `docs/handoff.md` と決まっているので中身を具体的に書ける
- **B. 全リポジトリ共通（`~/.claude/commands/`）** — どこでも打てるが、申し送りの置き場所がバラバラ
  （okuradi は `docs/handoff.md`、フリーライフは memory の `project_session_handoff_*`、r-hoso と ANR は決まっていない）。
  「探しに行く」処理が要り、当たり外れが出る
- **C. 共通の骨を作ってリポジトリごとに上書き** — 一番きれいだが、**揃えるものがまだ okuradi にしかない**

**ユーザーの判断: 「実験を兼ねて A で」。**

### 決めたこと

**このコマンドの仕事は「申し送りと実物の突き合わせ」だけにする。**

Issue の一覧を出す処理は入れなかった。hook がもう出しているので、**同じものを2か所で数えると食い違う**。
hook が拾えないものは1つだけ（**Issue のコメント**）なので、そこだけ手順に入れた。
番組側（r-hoso）の返事はメッセージではなくコメントで来る。

**名前 `/始め` は仮置き。** あとから変えられる。

### やったこと

- `.claude/commands/始め.md` を新規（87行）
- `CLAUDE.md` の「作業の流れ」0 を `/始め` に（`/区切り` と対になるように）
- `docs/handoff.md` のコマンドの案内を2つ並べ、確かめていないことを1つ足した
- コミット3つ、PR #124。**文書と道具だけなのでレビュー担当は呼んでいない**

### 通してみた（コミット2つ目）

**作りっぱなしにせず、その場で手でなぞった。** 結果、**申し送りと実物は全部一致していた**
（開いている Issue 15件・番号も全部・開いている PR 無し・未コミットは `ep01/config.yml` だけ・
r-hoso からの新しいコメント無し）。

一致していたのは良かったが、**通したことで手順の曖昧さが3つ出た**。

1. **コメントを見る Issue の選び方が「心当たりがあれば」だった** →
   申し送りが「〜待ち」と書いている Issue を全部見る、に変えた
2. hook が出ていないときの打ち直しに、最近動いた Issue が無かった → 日付の出し方ごと足した
3. hook が出ているかを確かめる前に数字を使えてしまった →
   「申し送りの数字を hook の数字と思い込まない」を足した

### 困ったこと / 学んだこと

- **手順に「心当たりがあれば」と書いたら、それは手順になっていない。**
  1 は、day-4 に番組側の Issue を3時間40分見落としたときと**同じ選び方**だった。
  見落とす人が「心当たり」で選ぶので、見落としは直らない。**選ぶ条件を、申し送りの文面から決まる形にした**
- **書いた値が、その場で古くなった。** 申し送りに「`/始め` はまだ1度も通していない」と書いた直後に手で通した。
  3つ目のコミットで「中身は手でなぞった / コマンドとしてはまだ呼べていない」の区別に直した。
  **day-4 の冒頭が古くなったのと同じ形**（書いた瞬間に自分で古くする）。**2回目**
- **「確かめたつもり」をもう一歩で踏むところだった。** このセッションから `/始め` を呼んだら見つからなかったが、
  **セッションを始めた場所が `~/workspace` で、起動時に okuradi にいなかった**。
  「再起動が要る」の証明にはならないので、**結論にせず未確認に入れた**
- **同じワークスペースで Claude を2つ起動していた**とユーザーから指摘があった。
  決めつけずに `git fetch` と `gh pr list` で見たら、**もう一方は何も push していなかった**。
  ブランチも PR も増えていない
- **作った道具は、その場で1度通す。** 通さなければ、上の3つは次のセッションまで見つからなかった

## #125 docs: day-5 議事録

#124 をマージする前提で書いた議事録。**この節を足したのが2回目の区切り。**

冒頭に「PR 1件」と書いたが、その後 #126 が入って 3件になった。
**書いた時点では正しく、区切りのたびに測り直す形なので追いかけない**（day-4 で決めた形）。

## #126 docs: 通しリハーサル（#53）のアプリ側の手順を書く

### きっかけ

ユーザーが「#53 をやる。段取りは r-hoso で決めてくる」と離席した。
待っている間にできることとして、**アプリ側の手順**を提案して通った。
番組側の段取り（何を録るか・構成）とは別物で、こちらの担当。

### 途中で止まったこと: 同じ作業フォルダに2つのセッション

書き始めてすぐ、**作業フォルダのブランチが `main` から `docs/start-command` に変わり、
自分が作っていないコミットが入った**。`ListAgents` で見たら `okuradi-a6` が動いていた。

**ここでブランチを作ると、相手の作業中のファイルが足元から入れ替わる。**
文書を作業用の場所（scratchpad）に置いて、ユーザーに判断を求めた。
ユーザーは「別のワークスペースでやっているつもりだった」とのことで、片付けを待ってから PR にした。

### レビューで5つ出た（うち4つは事実の間違い）

`user-reviewer` を呼んだ。**指摘は全部コードで裏取りしてから採用した。**

| 指摘 | 実物 |
|---|---|
| 「確定」というボタンが無い | `app.js:1044` は「**確認した**」。しかも押さなくても次に進める |
| エコーの順番が逆 | **整音を1度実行しないと波形が出ない**（`media.py:111`） |
| 「古い」に戻ることが書いていない | エコーを保存すると `_why_stale` が効く |
| コーナー名が弾かれるのは起きない | コーナーは選択式（`seriesSelect`）。無い名前は選べない |
| 手前の一手が抜けている | 「中止」「もう一度実行」がある |

**4つは、実物を見ずに書いた。** とくに「整音は2回押す」は、知らないと当日まず止まる。

経過時間の説明も直した。「次の工程を始めると消える」と書いていたが、
実物は**完了して5秒で消える**（`setTimeout(..., 5000)`）。控える余裕はもっと短い。

## #127 コーナーの尺は固定ではない（番組側の決定）

**申し送りが予告していたとおり、r-hoso の返事は Issue として来た。** メッセージではない。

- 決まったこと: **守るのは合計30分だけ。コーナーごとの尺は回ごとに動かす**（自作曲の長さが毎回変わるため）
- アプリへのお願いは1つ:「**コーナーの尺を定数で持たないでほしい**」

調べたら、`build.py` と `web/` に**尺の決め打ちは無かった**。数字で残っている「10分」は
`claude -p` の待ち時間（`timeout=600`）だけで、番組の尺とは無関係。

**いま直すものが無いので閉じ、#79（mix 工程）に引き継いだ。**
ラベルが付いていなかったので、**ラベルの話は別コメントに分けて書いた**（ユーザーの指示）。
決めずに置いてある。

## #106 章はコーナーと1対1（番組側の決定と、同じ日の訂正）

### 届いて、訂正された

昼過ぎに「**章は8個**（アバン・OP・4コーナー・告知・ED）」と届いた。
その数時間後に「**8個は間違い。正しくはコーナーと1対1で、数はコーナーの数に従う**」と訂正が来た。
番組側がアバンをやめたため、**自分で書いた数がその日のうちに嘘になった**。

番組側の言葉: 「`build.py` に固定の数を書くと、#127 の『尺を定数で持つな』と同じ問題が起きる」。

### アプリ側で見つけたズレ

**番組側の言う「コーナー」と、アプリの `segments` が指すものが違っていた。**

| | 中身 |
|---|---|
| 番組側の章（ep01・7つ） | OP / 今さら聞けない / ランテック通信 / 今月のパワープレイ / ざっくりITニュース / 告知 / ED |
| アプリの `series_rules`（4つ） | 今さら聞けない / ランテック通信 / 今月のパワープレイ / ざっくりITニュース |

**OP・告知・ED がアプリ側に存在しない。** `segments` の数をそのまま渡すと、多くても4にしかならない。

### 決めたこと（ユーザーの判断: 「A で行きましょう」）

**`series_rules` に OP・告知・ED を足し、`segments` に番組の並びとして並べる。**
章の数は `segments` を数えれば出るので、**`build.py` に数を書かない。**

落とした案:

- **B. 指示文で言葉として伝える** — 1行で済むが**数を保証できない**
- **C. 数の指定をやめ「コーナーと1対1」とだけ書く** — OP・告知・ED が章になるかが運任せ

B と C は「アプリは知らないが Claude は知っている」状態を作る。
次に構成が変わったとき、どこを直せばいいか分からなくなる。

**A の弱点は手間**（毎回 OP・告知・ED を並べる）。初期値にする手はあるが、**作ってから決める。**

**実装はまだしていない。** 触る所は #106 のコメントに書いた。

## 確かめられたこと

**day-4 から持ち越していた未確認が2つ解けた。**

- **始まるときの hook は発火した。** このセッションの頭に、Issue 15件と未コミットの変更が出た
- **`.claude/commands/` のコマンドは、立ち上げ直さずに拾われた。** #124 をマージした直後に `/始め` が呼べた

ただし途中で**一度間違えた**。#124 をマージする前に `/始め` を試して「呼べない」と出たので
報告したが、**ブランチを `main` に戻した時点でファイルが作業フォルダから消えていただけ**だった。
「呼べない」の理由が別にあった。

## 困ったこと / 学んだこと

- **同じ作業フォルダで2つのセッションを動かすと、ブランチの取り合いになる。**
  コミットが入ったことには気づけたが、**気づかずにブランチを作っていたら相手を壊していた**
- **件数を引き違えた。** #127 を閉じた後に「14件」と書いたが、**立てる前が15件だったので15件のまま**。
  day-4 の「件数を間違えた」と**同じ種類で2回目**。数え直さずに引き算した
- **実物を見ずに画面の説明を書いた。** ボタン名「確定」は存在しなかった。
  `CLAUDE.md` に「実物で確かめる」と書いてあるのに、**手順書を書くときに適用しなかった**
- **「呼べない」の理由を、確かめる前に決めかけた。** day-4 の「切り分ける前に原因を書かない」と同じ形。
  今回は報告する前に `ls` を見たので、**理由が別だと分かった**
- **番組側の決定は、同じ日に訂正されることがある。**
  「8個」を受けて実装していたら、数時間で作り直しになっていた。
  **区切りを先にしたので、実装が訂正に追いつかれずに済んだ**

## #129 feat: 章をその回のコーナーと1対1にする（#106 の案A）

### きっかけ

`/始め` で申し送りと実物を突き合わせたら、**全部一致していた**（Issue 15件・番号・開いている PR 無し・
手元は `ep01/config.yml` だけ）。番組側の新しい動きも無し。**#106 が、Claude が動ける唯一の Issue**だった。

`/開始` と打たれたが、そのコマンドは無い。`/始め` に読み替えて実行した。

### 検討したこと（実装時に決めてよいとされていた分）

#106 は「`title_hint` の書き方は実装時に決める」としていた。2つ決めた。**どちらも仮置き。**

- **OP・告知・ED に `title_hint` を書かない。** 回のタイトルを決めるコーナーではないため。
  代わりに `step_meta` で、**空の `title_hint` は「タイトルの規則」に出さない**ようにした。
  空の行を出すと「規則が無い」のか「書き忘れた」のか分からなくなる
- **`series_rules` の並びを番組順にする**（OP を先頭、告知・ED を末尾）

### 決めたこと（ユーザーの判断）

**手元の `ep01/config.yml` に残っていたエコー区間（`10.69〜14.01 / light`）は「捨てる」。**
day-4 に `git reset --hard` で消してしまったものと同じ値。今回は**捨てる前に中身を見せて聞いた**。

### やったこと

- `ep01/config.yml` の `series_rules` に `op` / `announce` / `ed` を足した
- `META_INSTRUCTION` の「3〜6個」をやめ、「この回のコーナーと1対1（同じ数・同じ順番）」にした
- `docs/rehearsal.md` の「3〜6個に決め打ち」の注意を、いまの動きに書き換えた
- `tests/test_meta.py` に指示文のテストを3件
- **実際の音声で meta 工程を通した。章が 4個 → 1個になった**（ep01 のコーナーは1つなので正しい）

```
前: 0:00 テスト録音の案内 / 0:08 雑談(疲れた話) / 0:15 鼻歌のような断片 / 0:53 締めの一言
後: 0:00 今さら聞けない OSI参照モデルの7層
```

### レビュー（code / scope / user）

**🚨 は1件だけ。** `user-reviewer` が実際にサーバーを立てて見つけた。

**`series_rules` の先頭に `op` を置いたせいで、「コーナーを追加」で足した行の初期値が「OP」になった**
（`web/static/app.js:2144` が `Object.keys(state.series)[0]` を使う）。

**裏取りしたら、指摘の半分は #129 のせいではなかった。**「変え忘れても保存時に弾かれない」は
`validate_segments` が重複を見ていないからで、**PR の前から同じ**。初期値も、前後どちらでも
「足した行の大半では間違い」である点は変わらない。**ユーザーの判断で、並びは番組順のまま残した。**

**2人が同じことを言った指摘が2つ**（scope と user）。

- OP・告知・ED はテーマが必須（`web/episodes.py:289`）なのに、書く手がかりが無い。
  `hint` が空になるので `app.js:2035` のフォールバック「例: OSI参照モデルの7層」が OP の欄に出る
- 新しい回のたびに並べ直す手間（`create_episode` は `segments` を引き継がない）

`scope-reviewer` はさらに、**案Aを選んだ理由「B・C は数を保証できない」が、まだ保証になっていない**
と指摘した。`pending_issues`（`web/media.py:414`）は章の数を `segments` と突き合わせていないので、
いまの動きは C とほぼ同じ。**A の利点を使うには、保留チェックに一手が要る。**

`user-reviewer` は **YouTube が章3件未満だと目次を出さない**ことも挙げた。
画面のコピー欄には「＋目次1件」と出る（`app.js:523`）ので、貼った先で章にならないことに気づけない。

### 直したこと・残したこと

ユーザーの判断で、**`docs/rehearsal.md` に2行足した**（章の見出しの言葉は未決 / YouTube の3件）。
**それ以外は #106 のコメントに6つまとめた**（Issue は増やさない）。

### 困ったこと / 学んだこと

- **レビューの指摘は、半分が「この PR のせい」ではなかった。** 🚨 として出てきたものを
  そのまま直していたら、**前からある性質を #129 のせいにして、関係ない所を変えていた**。
  `CLAUDE.md` の「確かめてから採用する」が効いた
- **画面の文言を書くときに、実物を見た。** 「＋目次1件」を `app.js:523` で確かめてから書いた。
  day-5 の「確定」の事故（同じ日の昼）を踏まえた

## #130 chore: 議事録の失敗パターンから、守りを3つ足す

### きっかけ

ユーザーの依頼。「**デブログ分析から今までの失敗パターンを分析し、それの対策のスキルや
カスタムコマンド、mcp 何かを作れないか検討したい**」。

day-1〜day-5 を全部読んだ（day-3 は「困ったこと / 学んだこと」の節だけ抜き出した）。

### 分かったこと（1つ）

**「気をつける」と書いた失敗は繰り返した。機械が止めた失敗は止まった。**

| | 回数 | いま |
|---|---|---|
| 実物を見ずに書いた | **9回**（day-3 に5・day-4 に3・day-5 に1） | `CLAUDE.md` に最初から書いてあるが減っていない |
| 件数を数え直さず引き算した | **2回**（day-4・day-5） | day-4 で対策を書いた後に再発 |
| git の強い操作の事故 | 2回（day-4） | **hook を作った後は出ていない** |
| hook 自身の穴 | 2回（#110・#112） | **テストにした後は出ていない** |

day-4 の議事録が自分で「手順にしても『気をつける』と書くだけで効かない。テストが正解」と
結論しているとおりの結果だった。**ただし「止まった」の根拠は day-5 の1日分だけ**で、
day-5 は実装をほとんどしていない日なので、git を強く操作する場面自体が少なかった可能性がある。

**ユーザーの言葉: 「気を付けるだけだと失敗するのは人間も機械も一緒なのは安心しました。
しかし、仕組みを大量に作ることでボトルネックになるのなら考え直します。」**

### 検討したこと

最初に A / B / C を出した。ユーザーが「全部やる」と言った後、**ボトルネックになるかを測り直した。**

| | 中身 | 測った結果 |
|---|---|---|
| A | レビュー担当に裏取りを要求 | **新しいファイル0・新しい手順0。** 既存のレビューの質が変わるだけ |
| B | 申し送りの数字を機械が数える | `/区切り` はすでに `day-stats.sh` を呼んでいた。**足すだけ。0.4秒** |
| C | 消した値が他に残っていないか探す | **唯一、新しい道具になる** |

**C は試作して測った。** 10行の版を今日の差分に当てたら、**探したかった「3〜6個」が出てこなかった。**
消した行を `^-[^-]` で拾っていたため、Markdown の箇条書き（`-- ` で始まる）が全部こぼれていた。
**day-4 の「守りは、分からないときに通す方へ倒れる」と同じ形。**

直したら出た（`3〜6個` 6件 / `append(` 158件＝騒音）。

### 編集長モデルの検討（採らなかった）

ユーザーの案。「**フリーライフ制作でやっている編集長ポジション。編集長=新エージェントの下に
各レビュアーがいるイメージ。ただアプリ開発となろう小説制作ではやり方がだいぶ違う**」。

**確かめた事実**: いまの8人の担当には、**どれにも `Agent` ツールが無い**（`tools: Bash, Read, Grep, Glob`）。
今のままでは編集長は部下を呼べない。**与えられるかは確かめていない。**

仕組みより大きい違いを挙げた。**小説では編集長の仕事は「味の判断」で、機械で確かめられない。
アプリでは「本当に壊れるか」は動かせば分かる。** 実際この日、#129 のレビューで
「🚨 の半分は前からある性質」と分かったのは、**報告をそのまま読めたから**だった。
編集長が間に入ると、確かめる相手が「コード」から「編集長の要約」に変わる。

また、**圧縮の必要がもう無い**ことも測って示した。レビュー3人は
**35,934 / 53,476 / 84,703 トークン**使ったが、こちらに届いたのは報告だけだった。

**挟む価値があるのは「人が読めない長さの、機械の出力」**（＝C）で、レビュアーの報告はそうではない。
**ユーザーの判断: 「編集長はやめて、A・B・B' を今日やりましょう」。**

### B' が途中で生まれた

ユーザーの「**code-reviewer のいくつか見ていることをこの新エージェントに引き継がす。
必要な時がほぼ毎回あったほうがいい部分を任せる**」から。

観点8つを数えたら、「ほぼ毎回要る」のは2つだけだった。そして調べたら、
**この2つは担当に渡すより機械に落とせる。しかもどちらも守られていなかった。**

| 約束 | 場所 | 壊れると | テスト |
|---|---|---|---|
| Claude はサブスクの枠で呼ぶ | `build.py:559` が `ANTHROPIC_API_KEY` を外す | **従量課金で実費が出る** | **無かった** |
| 認証情報と音声は git に入れない | `.gitignore` | **公開リポジトリに出る** | **無かった** |

**引き継ぎ先は新エージェントではなく `tests/` だった。**

### やったこと

- `code-reviewer.md` / `user-reviewer.md` に「出す前に実物で裏を取る。取れなかったものは
  指摘にせず『確かめられなかったこと』に書く」を足した（A）
- `day-stats.sh` が `pytest` の件数も出すようにした（B）
- `tests/test_guards.py` を新規・11件（B'）
- **穴を開けた版に当てて、狙った7件だけが落ちることを確かめた。** `pytest` は 246件

### 作らなかったもの（測って決めた）

- **画面の文言の自動照合** — day-5 の「確定を押す」は `git log -S` で探すと**コミットされておらず**、
  レビューが先に捕まえていた。いま `docs/rehearsal.md` の太字の画面文言6語は**6語とも実在**し、
  当たる対象が無い
- **`file:line` の参照の検査** — 文書全体で参照は**1つだけ**だった
- **MCP** — 議事録の失敗は全部、手元の git・`gh`・ファイルの話で、いまの `Bash` で届く

### 困ったこと / 学んだこと

- **「該当なし」と報告したが、実際にはあった。** `code-reviewer.md` に裏取りの要求が無いと書いたが、
  出力の節に「ファイル:行」を書く要求があった。**grep のパターンが全角コロンに当たっていなかった。**
  day-4 の「範囲を絞った grep で『全部見た』と書いた」と**同じ形**。
  ただし今回は**編集する前に全文を開いた**ので、直す内容は正しくなった
- **「いまも起きています」と書いたが、起きていなかった。** 申し送りの「pytest 232件」を
  実測 235件とずれていると書いたが、**235件は #129 のブランチの数**で、main では 232件が正しかった。
  **測った場所が違った。** day-4 の「別のブランチの古い版を測った」と同じ形。
  B の根拠自体は残る（day-5 の申し送りに「前の申し送りの 225件は古い」と書いてある）
- **自分で作った検査が、狙ったものを取り逃した。** C の試作の1行目。
  **守りを作ったら、当てるべきものに当たるかを先に試す**（day-4 の #107 と同じ）
- **ユーザーの案の方が良いことがあった。** 「騒音を裁く係を挟む」は、こちらの
  「レビュー担当に畳む」案より良い。**畳む案は、騒音を減らすために取りこぼしを増やす形**だった。
  担当を挟めば、機械の側を「迷ったら出す」に倒せる

## 次にやること

1. **#53 通しリハーサル**（ユーザー）。手順は `docs/rehearsal.md`。**v1 の最後の関門**
2. **#132 を起票した**（この区切りの直後）。中身は
   「機械＝迷ったら出す / 担当＝裏取り / メイン＝短い一覧」の3分割と、
   **最初の手順として「過去の PR に当てて、何行出るか・担当が何分かかるかを測る」**。
   **着手は #53 を通してから。** これで開いている Issue は **16件**になった
3. **設計の Issue には着手しない。** #53 を通してから
