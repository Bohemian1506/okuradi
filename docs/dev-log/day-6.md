# Day 6（2026-09-22）通しリハーサルを空打ちし、番組側の決定で方針が変わった日

**マージした PR 20件・文書 796行増81行減・それ以外 1426行増24行減。`pytest` 246 → 327。**

数字は区切りのたびに `docs/dev-log/day-stats.sh` で測り直す。**前の版から写さない。**
この節は1日で何度も古くなった（朝「PR 2件」→ 夕方「5件」→ 夜「11件」→「18件」→ いま「20件」）。
**この議事録の PR は、まだマージしていないので入っていない。**

- PR 20件のうち **1件（#134）は前夜の day-5 の区切りの PR**（手元の時刻で 01:00 にマージ）
- **議事録の PR が8本ある**（#139 #148 #149 #150 #152 #156 #158 と、これ）。
  **「マージしたら議事録」を守るとそうなる**。夕方までは守れておらず、#151 でそれを知らせる hook を作った

**day-5 の続きで0時を回った分（#79・#88 のコメント）は、コミットが無いので PR にも数字にも出ない。** 下の「前夜の続き」に書く。

**この日は、途中で方針が変わった。**

午前は「**#53 を通してから 30分番組化に着手する**」で動いていた。リハーサルの空打ち（#136）も、
#135 と #137 を見送ったのも、全部その理由だった。

午後、**番組側から「今さら聞けない（8分枠）は、収録した wav ではなく合成音声が丸ごと入る」という決定が届いた。**
ep01 で収録音と合成音の2本ができることになり、**いまのアプリは音源を1本しか扱えない。**
ユーザーの判断で **「GUI まで入れる。レーンまで作る。9/24 への遅れは許容する」**に変わった。

その結果、**この日のうちに #87 を閉じ、#79 の論点1 を決めた。**

---

## 前夜の続き（コミット無し）

day-5 の区切りの後、**番組側（r-hoso）から #79 に決定が届いた**。「合いの手は台本に書かない。録ってから文字起こしを見て場所を決める。**どう実現するかはアプリ側で決めてください**」。

受けて、FFmpeg で実測した（#79・#88 のコメント）。**重ねる（`amix`）なら尺が1サンプルも変わらず、無音を差すとその長さぶんちょうど伸びる。**

**この日、その続きとして「差し込みで作る」を #79 に書いた。**

- きっかけ: 決定は届いていたが、**「重ねる予定はない」というユーザーの言葉が申し送りにしか無かった。**
  このままだと次のセッションで同じ調査をやり直す
- 決めたこと: **技術的には重ねる方が有利だが、差し込みを採る。** 番組が被せることを想定していないから。
  **番組の形がアプリの作りを決める。逆ではない**
- 決めたことの波及: 論点2（文字起こしは mix の前）が「選べる案」ではなく「そうするしかない案」になった。
  論点7で `video` の依存を `clean` から `mix` に移す必要が出る
- **及ばない範囲も書いた。** 決めたのは合いの手だけで、SE の重ね（ピンポーン・ブッブー）は今までどおり重ねる

---

## #136 通しリハーサルの手順書を、実際に画面で通した結果に合わせる

- **きっかけ**: 申し送りの「まだ確かめていないこと」に **「`docs/rehearsal.md` が当日ほんとうに役に立つか。コードで裏取りして書いたが、実際に通していない」**があった。
  #53 は本人が 9/24 の収録でやる番で、Claude がいますぐ着手できる Issue は無い。
  **本番を待たずに空打ちすれば、手順書の穴だけ先に潰せる**
- **検討したこと**:
  - `ep01` で直接通す案は採らなかった。**手順書自身が「`ep01` は試験用。上書きしない」と書いている**
  - 捨て回（`ep99`）に `ep01` の収録音をコピーして通した。**通した後に消した**
  - 画面は playwright（ヘッドレス Chrome）で操作した。#86 / #97 と同じやり方
- **やったこと**: **6工程すべて通った**（音源 → 下見 → 整音（エコー付き）→ 文字起こし → メタデータ → 動画 → コピー）。
  手順書と実物の食い違いを2つ直し、レビューで出た3つを足した
  - テーマが空だと回を作れない（「止まりやすい所」に同じ番号のことしか無かった）
  - 経過時間は「開き直しても残る」。**ただし残るのは最後に動かした1つだけ**（`runner.py` の `_current` はグローバル変数1個）
  - OP・告知・ED にもテーマが要る / 画面3 の工程も裏で走る / エコー保存の直後ダイヤルは「完了」のまま
- **レビュー**: `code-reviewer` と `user-reviewer` を呼んだ（変更は文書1ファイルだが、**書いてあるのは「画面はこう動く」なので、コードとの突き合わせが要る**）。
  **🚨 は0件、🟡 が5件。全部採った**

### 困ったこと / 学んだこと

**同じ形の取り違えを、1日で2回やった。どちらも「見る場所を間違えた」。**

| | 何を間違えたか |
|---|---|
| 1 | 手順書の「**保存する**」が画面に無いと思い、「保存」に直しかけた。**実物は、区間を選んだ直後だけ「保存する」**（`app.js` の `dirty ? "保存する" : "保存"`）。整音の直後に見たので、保存済みの状態の「保存」しか見えていなかった |
| 2 | 「整音の結果が**古い**に変わる」が起きないと思った。**見るのはダイヤルではなく整音のパネル**。ダイヤルの判定（`step_states`）は音源の更新日時しか見ず、パネル（`_why_stale`）はエコーの変更も見る。**「古い」が2つある** |

**どちらも、採用する前にコードと実物で裏を取ったから止まった。** 1つ目は `grep` で `app.js` の
`dirty ? "保存する" : "保存"` に当たって気づき、実物でも確かめ直した（ドラッグ前は「保存」、ドラッグ後は「保存する」）。

**day-5 の「測る場所を間違えない」と同じ形で、これが3回目。**
前の2回は「別ブランチの件数を測った」「全角コロンに grep が当たらなかった」。
**今回は「画面の別の場所を見た」「別の状態のときに見た」。**
共通しているのは、**測る前に「どこを・どの状態で見るか」を決めていない**こと。

もう1つ。**レビュー担当を呼んだ判断はよかった。** 5件のうち2件は**自分が今日書いた文**への指摘で、
とくに「開き直しても残る」は**嘘ではないが読み手を間違った方へ連れていく**書き方だった。自分では気づけなかった。

---

## #135 PC 再起動後に start.sh を実行すると、Claude が2つ立つ（起票）

- **きっかけ**: 朝、ユーザーが「同じセッションが二つ生えてるんですがこれなんでですかね？」と言った
- **調べたこと**: Herdr のサーバーログに全部残っていた。
  **16:06:32 にサーバーが起動してペインの復元を始め、16:06:34 に `start.sh` が「空いているペインが無い」と判断し、
  16:06:35 にペインを割って `lead` を起こし、16:06:36 に復元が終わった。2秒の差**
- **原因**: `start.sh` は「サーバーが応答するか」しか待っていない。**ペインの復元が終わったかは待っていない。**
  `pane_is_idle_shell` が「まだ生えていない」と「使用中」を区別せず、どちらも「空いていない」に倒している
- **決めたこと**: **直さずに Issue にする。** 次の再起動でまた起きるが、実害は「余分な Claude が1つ立つ」だけ。
  #53 を通してから直す
- **片付け**: 空の `lead`（一度も話しかけられていない）を閉じ、動いていた方を `lead` に名付け直した

**学んだこと: 「なんか変なことやったんでしょうか」と聞かれたとき、答えはログにあった。**
心当たりで答えず、時刻の並びを出したから原因が特定できた。

---

## #138 番組側の「構成」の表を貼り付けて、コーナーの並びとテーマを埋める（起票）

- **きっかけ**: 手順書に「OP・告知・ED にもテーマが要る」を足したところ、
  ユーザーが **「台本を見て作る方式にするのが楽かなと思うのですがどうでしょうか？」**と言った
- **前提がずれていたので直した**: **テーマに当たるものは台本ではなく「構成」にある。**
  `r-hoso/01_構成/epNN.md` の表の「この回で扱うもの」の列がそれ。`02_台本/` にあるのは尺の表と本文
- **実測してから書いた**（`CLAUDE.md`「Issue を書く前に実機で測る」）。捨て回に ep01 の構成の7コーナーを手で入れた。
  **7行とも入り、保存できた。操作は21回**。詰まって進めなくなるわけではない
  - **足した行の既定は必ず「OP」。7行のうち6行でプルダウンを変える**
  - **コーナーが重なっていても保存できる**（検査は空のテーマしか見ていない）
  - **OP・告知・ED のテーマ欄に「例: OSI参照モデルの7層」と出る。** 何を書けばいいか分からない所に、関係ない例
- **検討したこと**: 貼り付け（A）／ r-hoso を直接読む（B）／ `claude -p` に読ませる（C）
- **決めたこと**: **A を採る。** B は okuradi が r-hoso の書き方に依存し、
  **番組側が構成の形を変えた日にアプリが壊れ、困るのが収録直後の本人になる**。
  `CLAUDE.md` の「番組側とはやりとりするが、リポジトリは別」という向きを崩さない
- **尺の列は拾わないと書いた。** #79 のコメントで「尺を `segments` に持たせるかは mix を作るときに決める」となっているため

---

---

## 番組側から決定が2件届いた（午後）

**呼び鈴はメッセージで来たが、正本は Issue だった。** 読みに行って中身を確かめた。

| | 中身 |
|---|---|
| **#137（新規）** | 「今さら聞けない」を**ずんだもん（VOICEVOX）**が喋る。**規約でクレジットが要る**ので概要欄に入れる |
| **#79 のコメント** | **そのコーナーは合成音声が1コーナー丸ごと**（実測7分53秒）。mix で収録音と混ざる |

### #137 の扱い（アプリ側で決めた）

- **入れ方**: `youtube_description`（`build.py:583-594`）が**機械で必ず足す**。`claude -p` には書かせない。
  **規約の義務なので、入らなかった回が出ても気づけない作りにしない**
- **9/24 には仕組みを間に合わせない。** 手順書に1行足して手で貼る（#140 でマージ済み）。
  **リハーサル直前に工程や画面をいじると、当日詰まったときの切り分けができなくなる**（#135 を見送ったのと同じ理由）

### #79 のコメントの受け止め

**いちばん大きいのは「このコーナーは文字起こしをかける意味が薄い」こと。**
台本の文字がもう分かっている（2198字）のに Whisper にかけると、**誤認識が混じったものが返ってくる。**
聞き取る問題ではなく、**台本の文字に時刻を付ける**問題になる。#88 に効く。

## 方針が変わった: レーンまで作る

- **きっかけ**: ep01 に収録音と合成音の2本ができる。`find_raw`（`build.py:121`）は**先頭1つ**しか使わず、
  `_clear_raw`（`web/sources.py:126`）は**新しい音源を入れる前に 00_raw を空にする**。
  GUI は「1本しか置けない」を積極的に守っている
- **実測してから案を出した**: 素直に繋ぐ（`-f concat`）と、**エラーを出さずに壊れた。**
  24kHz の合成音を 48kHz として再生し、**10秒が5秒になった（倍速・1オクターブ上）**。
  `filter_complex` でレートをそろえると `duration_ts` までぴったり一致した
- **これで見え方が変わった。** はじめ「アプリの外で繋ぐ」を勧めたが、
  **壊れ方が「黙って倍速」**なので、手順の注意書きで防ぐ種類ではない。`CLAUDE.md`「静かに失敗させない」
- **ユーザーの判断**: 「**GUI までいれましょう。これによる遅れは許容とします**」
- さらに「**長尺のボイスロイドレーンを作る、というのはダメなんでしょうか？**」から、
  **レーンまで作る**ことになった

**このとき、ユーザーの記憶のほうが正しかった。**
こちらは「数十レーン問題は未解決」と言ったが、**#86 で解決済みだった**（案E'・3行）。
**#86 の本文だけ読んで、決着のコメントを読んでいなかった。**
`CLAUDE.md` の「**ヘッダや冒頭のサマリは更新漏れで古いことがある。末尾や本体を見る**」そのもの。

## #87 を閉じた（ライブラリと、読み込みの実測）

- **決めたこと1**: #3 の「外部ライブラリは波形表示の wavesurfer.js のみ可」を、
  **「wavesurfer.js と、その公式プラグイン」まで広げる**。#86 の決定が丸ごとこのライブラリの実測の上に建っているため
- **決めたこと2**: **読み込みは、いまの見せ方で足りる。** 波形をサーバーで作って渡す（#36 を戻す）必要は無い

**30分・音源49本・299MB・3行を実物大で組んで測った。**

| 測った場所 | 波形が全部描けた（冷えた状態） |
|---|---|
| WSL の中のブラウザ | 3.01秒 / 3.37秒 |
| **Windows のブラウザ（実際の使い方）** | **4.03秒 / 3.88秒 / 3.85秒** |

**#87 本文の「57秒×5レーンで15秒」は再現しなかった。#86 の「3.7〜4.0秒」が再現した。**

決まりは2か所に書いてあった（`docs/design-prompt.md` と `web/static/vendor/README.md`）。
**片方だけ直すと、もう片方が「増やすな」と言い続ける**ので両方直した（#141）。

## #79 の論点1 を決めた（`timeline.yml`）

- **決めたこと**: タイムラインは **`timeline.yml` という別ファイル**にする。
  `config.yml` は**人が書くもの**、`timeline.yml` は**画面がドラッグで書き換えるもの**
- **`echoes` と `cuts` は `timeline.yml` へ移す**（どちらも時刻を持つ編集）。`segments` は `config.yml` に残す（時刻を持たない）
- **`timeline.yml` は前の回から引き継がない**
- **ユーザーが「デメリットを潰せないか調べて」と言ったので調べた。3つのうち2つが消えた**

| デメリット | 調べた結果 |
|---|---|
| 新しい回を作る仕組みがもう1つ要る | **消える。** 引き継がないので写す仕組みが要らない。しかも #142 が構造的に直る |
| 「古い」の判定に穴がもう1つできる | **むしろ有利に転ぶ。** いまの `_why_stale` は26行の特別扱い。分ければ更新日時が正確な合図になる |
| 正本が2つになる | **機械で守れる。** `tests/test_guards.py` の `test_回のフォルダで追跡しているのはconfigymlだけ` が落ちて気づける |

**決まっていないものも書いた。** 論点1 は3つ聞いていて、**決まったのは置き場所とキーの分け方だけ。**
時刻の書き方（絶対か相対か）と、要素の型の分け方は**まだ決まっていない**。

## #142 起票（エコー区間の引き継ぎ）

- **再現した**: `ep98` にエコー区間を保存 → `ep99` を作ると同じ値が入る。
  `create_episode` が「全部写して一部を消す」作りで、**`echoes` が消す対象に入っていない**
- **単独では直さない。** 「消し忘れリストに1行足す」形は、**次にキーを足したときにまた漏れる**。
  `timeline.yml` に `echoes` が移り、そのファイルを引き継がなくなれば構造的に消える（ユーザーの判断）
- **いつ踏むかを、言い直した。** はじめ「9/24 に踏む」と書いたが、
  **ひな型になる `ep01` にエコー区間が無い**ので 9/24 は踏まない。**踏むのは `ep03` を作るとき**

## この日の失敗（測り方と、読む場所）

**5つあった。どれも「確かめる前に言った」か「測り方が間違っていた」。**

| | 何が起きたか | どう気づいたか |
|---|---|---|
| 1 | 手順書の「**保存する**」が無いと思い、直しかけた | `grep` で `dirty ? "保存する" : "保存"` に当たった |
| 2 | 「整音の結果が**古い**に変わる」を、**ダイヤル**で見た。正しくは整音のパネル | コードで2つの判定が別物だと分かった |
| 3 | **#86 の本文だけ読んで「数十レーン問題は未解決」と言った。** 決着のコメントを読んでいなかった | **ユーザーに指摘された** |
| 4 | **ライブラリが 404 のまま読み込みを測っていた。** それでも「canplay 0.3秒」ともっともらしい数字が出た | 波形が1枚も描けていないのがおかしいと思った |
| 5 | **波形の数え方が間違っていた**（wavesurfer は shadow DOM の中に描く） | 同上 |

**`canplay` だけを見ていたら「0.2秒」を採用していた。**
申し送りの「**名前を信じない。`canplay` は『再生できる』であって『波形が描けた』ではない**」が、そのまま当たった。

**道具でも1つやった。** `pkill -f "http.server 8777"` を打って、**パターンが自分のコマンド行に当たりシェルが落ちた。**
申し送りに「**`pkill -f` を使わない。`ps` で PID を拾って `kill` する**」と書いてある。**書いてあるのに使った。**

## #145 timeline.yml の読み書きと、錨から番組の時刻を出す計算（#144）

**この日はじめてコードを書いた。** ここまでは決定と文書だけだった。

- **きっかけ**: 「レーンまで作る」に方針が変わり、#87 が閉じて前提が立った。
  #85 の実装に入るには、**クリップの位置をどこにどう書くか**（#79 の論点1）が要る
- **やったこと**: `web/timeline.py` と `tests/test_timeline.py`。**工程にも画面にも繋いでいない**
  - 分けた理由: 繋ぐ側は `find_raw` を呼ぶ3か所と `web/sources.py` の `_clear_raw` に触る。
    **データの形が固まる前にそこを触ると、どちらのせいで壊れたか分からなくなる**

### 決めたこと: 錨（いかり）で持つ

**保存するのは「どのクリップの、先頭から何秒か」だけ。番組の先頭からの秒数は計算して出す。**

絶対秒で持つと、**コーナーを録り直すたびに後ろ全部の数字を書き直す**ことになる（#79 本文の「避けたい」形）。
もう1つ、絶対秒だけでは **「その SE はコーナーの12秒の所に付いていたのか、番組の8分20秒の所に付いていたのか」が
区別できない**ので、詰め直しの正解が決まらない。

実測値で確かめた（収録音 57.114938秒 / ずんだもん 473.308秒）。**収録を10秒伸ばすと、後ろのクリップも、
そこに付いた SE も、きっかり10秒動く。**

### 同じ日のうちに決め直した

**最初の決定に穴があった。** レビューで見つかった。

`cuts` と `echoes` を別々の並びで持つ形にしていたが、**この2つは時刻の原点が違う。**
工程が `scan → cut → clean` で、`step_cut` は生音を読み、`trimmed.wav` はカットの後に作られる
（`build.py:252` / `build.py:432`）。**カットは長さも変える**ので、
README の「時刻の基準は、その編集で長さが変わらない音に置く」がカットだけ当てはまらない。

**ユーザーの案で、Premiere などと同じ分け方にした。**

| | 何が入るか | 何で数えるか |
|---|---|---|
| **素材に対する指示** | カット・エコー（`edits` という**1つの並び**に `kind` を付ける） | **生音の時刻** |
| **出来上がりに対する位置** | クリップの配置・SE・文字起こし・章 | **出来上がった音の時刻** |

ユーザーの言葉:「**それかプレミアプロのように編集点を作ってその範囲をエコーかカットか選べるようにするって仕組みは作れます？**」

**この形にすると、原点の食い違いが消えるだけでなく、下見を読みながらカット区間を引けるようになる。**
`build.py` の先頭に「下見の文字起こし（**カット点を探す用**）」と書いてあるのに、
カットが GUI に無いので、いまその使い方ができていない。

### レビューで直したこと

`code-reviewer` と `scope-reviewer`。**全部コードを動かして再現してから直した。**

| 指摘 | 何が起きていたか |
|---|---|
| 丸め方 | `positions` と `total_seconds` が**別々に足していて、0.0013秒 食い違っていた**（実測） |
| はみ出し | **50秒の音に 100〜9999秒のカットが素通りした。** #144 の受け入れ条件だったのに実装していなかった |
| 素通し | **`bgm` / `se` に区間を書くと、値の形すら確かめずに残った**（文字列でも通った） |
| 使われない口 | `_number` の `allow_zero`、`_ranges` の `keep_keys`。**先回りして作った引数が2つ** |

`scope-reviewer` は「**繋がないコードを先に入れる判断は妥当**」と言ったうえで、
「**次の PR で必ず繋ぐ**」を条件に挙げた。#147 で繋いだ。

### 困ったこと / 学んだこと

**決定を書いた同じ日に、その決定の穴をレビューで指摘された。**
#79 に書いた表を、数時間後に自分で取り消すことになった。

**取り消した決定は消さずに残した。** 「このコメントより前に書いた決定には穴がある。前の表は使わないこと」と
書いて、何が間違っていたかも書いた。**消すと、同じ間違いをまた考えることになる。**

## #146 決定3（途中のカットは後回し）を取り下げる

- **きっかけ**: #145 のレビューで、`scope-reviewer` が
  「**`docs/features.md` では途中のカットは『後回し』のままで、コードだけが先に持っている状態**」と指摘した
- **何が食い違っていたか**: `docs/features.md` の解除の条件は
  「**v1 を何回か使って、前後のトリムだけで足りるか分かってから**」。**#53 はまだ済んでいない。**
  一方 #79 には「画面から使えないから使っていない。出せば使う」と書いていた
- **決めたこと（ユーザー）**: **取り下げる。** #53 を待たない

### なぜ待たなくてよくなったか

**待っていた前提が、番組側の決定で変わった。**

「今さら聞けない」がずんだもんの合成音声丸ごとになり、**1回分に音源が2本以上できる**ことになった。
そのためタイムライン編集（#85）に入ることになり、**「区間を選んで、それが何かを決める」操作を作る**ことになった。

**その操作ができれば、カットはその使い道の1つにすぎない。**
エコー（すでにある）・カット・切り抜き（#90）が、同じ操作になる。

**もう1つ。「使っていないから後回し」が理由として成り立たなくなった。**
`ep01` の `cuts` が空なのは、**画面から使えないから**で、足りているからではない。

### 同じ決定が3か所にあった

- `docs/features.md`（正本。決定3・機能の表・「後回しにしたものを、いつ考えるか」）
- `README.md`（「途中のカットは当面やらないと決めたため」）
- `docs/components.md`（「**途中のカットはしない**（決定 3）」）

**1か所だけ直すと、残り2つが「やらない」と言い続ける。** grep で探して全部直した。
**履歴は消さず、取り消し線で残して現状を併記した。**

### 学んだこと

**決定を Issue にだけ書いて、正本を直していなかった。**
#79 のコメントに「`cut` 工程を GUI に出す」と書いたのは同じ日の午後。
**そのとき `docs/features.md` を見に行かなかった。**

`CLAUDE.md` の「**決めたことは、その場で正本に書く**」は、Issue だけでは足りない。
**Issue は決めた経緯、`docs/` は決まった結果**で、役割が違う。

**Claude によるカット候補の提案は、後回しのまま。** 人が区間を引く話と、機械が提案する話は別なので、
表の理由欄を「途中のカットとセット」から「切り離した」に直した。

## #147 timeline.yml の並びどおりに音源を繋ぐ

**この日の出発点に届いた回。** 朝「ep01 に音源が2本できる」と分かってから、夜に実際に繋げるようになった。

- **きっかけ**: #145 でデータの形が固まり、`scope-reviewer` が「**次の PR で必ず繋ぐ**」を条件に挙げていた
- **やったこと**: `find_raw` が `timeline.yml` を見て、本編に音源が2本以上並んでいれば
  繋いだ1本（`00_raw/joined.wav`）を返す。**呼び出し側（下見・カット・整音）は変えていない**

### 実物で通した

`ep01` の収録音（**57.114938秒**）と、番組側が作ったずんだもんの合成音声（**473.308秒**）を
捨て回に並べて、コマンドで整音まで通した。**繋いだ音は `duration_ts` が 25460301 で、足し算の期待値と完全一致。**

**`CLAUDE.md` の「完了は、変えた工程を実際の音声で通したとき」を、この日はじめて満たした。**
ここまでは全部、繋がっていないコードと文書だった。

### 形式は判定せず、常にそろえる

**24kHz の音を 48kHz として繋ぐと、ffmpeg は何も言わずに倍速・1オクターブ上の音を作る**（同日に実測。10秒が5秒）。
**判定を間違えると気づけない壊れ方**なので、「合っていたらそのまま」に分岐せず、常にそろえる方に倒した。

実物は 48kHz で来た（番組側が「**r-hoso 側できちんと 48kHz で出すようになっていました**」）。
**それでも判定に寄りかからない。** ジングル・SE・BGM の形式はまだ分かっていない。

### テスト担当を呼んだら、穴が2つ出た

**この日はじめて `test-writer` を呼んだ。** それまで全部自分で書いていた。
`CLAUDE.md` に「実装後のテスト追加、バグの再発防止テストで起動する」と書いてあるのに、**呼ぶのを忘れていた。**
ユーザーに「**テスト担当どうしましたっけ？**」と聞かれて気づいた。

| 出たもの | 何が起きていたか |
|---|---|
| **繋ぎ直しの判定** | `timeline.yml` の**並びを入れ替えても、行を1つ消しても繋ぎ直されない**。どちらも元の音源ファイルは変わらないので、その更新日時しか見ていない判定を素通りしていた |
| **`gap` の無音** | `join_sources` が `gap` を無視してただ繋いでいた。**計算 6.5秒・実際の音 5.0秒**（実測）。`positions` の時刻と音が食い違っていた |

**2つめは、落ちたテストではなく「確かめられなかったこと」に `gap` が挙がっていたので見に行って見つけた。**
#130 で足した「**裏を取れなかったものは、指摘にせず分けて書く**」が、こういう形で効いた。

### レビューで、自分の書いた理由が嘘だと分かった

`code-reviewer` の指摘2件。どちらも自分で再現した。

- **判定が `>=` だったので、更新日時がぴったり同じときに黙って古い音を返していた。**
  ext4 はナノ秒まで見るので普段は起きないが、exFAT / FAT32 は粒度が粗い。`>` にして安全側に倒した
- **「トップで読むと輪になる」というコメントが、実測では再現しなかった。**
  どちらの順に読んでも通る。**確かめずに理由を書いていた。** 関数の中に置くこと自体は続けるが、
  理由を「依存の向きを保つため」に直し、落ちないことも書いた

### 記録の事故: 議事録の PR にコードが混ざった

**この PR は、それ自体をマージしていない。** 議事録の PR（#148）を作るとき、
**`main` に戻らずに `feature/join-sources` の上から新しいブランチを切った。**
そのため #148 に `build.py` と `tests/test_join.py` が丸ごと入り、
**#148 のマージで、この PR の中身も main に入った。**

**`/区切り` の手順に `git switch main && git pull` してから `git switch -c` と書いてある。そこを飛ばした。**

中身は問題ない（レビュー済み・295件通過・実音源で通した）が、**記録として誤解を招く**ので
#147 と #148 の両方にコメントを残した。

**直し方は「ブランチを切る前に `git branch --show-current` を見る」。** 申し送りにも書いてある。

## #151 PR をマージしたら、議事録を知らせる hook

**この日の自分の失敗を、仕組みで受け止めた回。**

- **きっかけ**: ユーザーに「**マージごとに議事録を作るルールは消えてしまいましたかね？**」と聞かれた。
  消えていない。**私が飛ばしていた。**
  #145 と #146 をマージしたあと、議事録を書かずに #147 の実装に入っていた
- **決めたこと（ユーザーの問い「**このルール読み飛ばしたことの対策を仕組みで解決できますか？**」から）**:
  **マージの直後に知らせる。止めない**

### なぜ止めないか

はじめは「**議事録を書くまで、次のマージを通さない**」形を考えた。**やめた。**

`CLAUDE.md` に「**誤字直しなど、経緯を残すほどでない PR は省いてよい**」とある。
**機械は「書いたか」は見られるが、「省いてよい PR か」は見られない。**
一律に止めると、省いてよい場面まで止まる。

そして**私の失敗は「忘れた」**なので、知らせれば足りる。

**逃げ道は抜け道にもなる**ことも、作る前に言った。議事録を1文字だけ足してコミットすれば通る形になる。
**この仕組みは、忘れるのは防げるが、手を抜くのは防げない。**

### 壊れ方を先に決めた

| | なぜ |
|---|---|
| **`gh` を呼ばない** | 固まると毎回それだけ待つ（始まるときの hook が `gh` で45秒かかった件） |
| **`jq` が無くても黙って通す** | 止める守りとは違い、動かなくても作業は進むべき |
| **ヒアドキュメントの中身は読み飛ばす** | 無いと **PR 本文やコミットメッセージに書いただけで知らせが出て、うるさくて読まれなくなる** |

最後のは既存の2つ（`block-main-push.sh` / `block-hard-reset.sh`）から持ってきた。
**day-4 に、ヒアストリングを読み違えて後ろの本物が素通りする穴**が見つかっている場所でもある。

### 行きすぎも試した

**知らせる3つ・黙る6つ**をテストにした。「**止めないこと**」自体もテストにした。

### 知らせの中身に、もう1つの失敗への手当ても入れた

**「ブランチは `main` に戻ってから切る」**を知らせに入れた。

この日、**`main` に戻らずに `feature/join-sources` の上から議事録のブランチを切り**、
議事録の PR（#148）に `build.py` と `tests/test_join.py` が丸ごと入った。
**そのマージで #147 の中身が main に入った**（#147 のコメントに経緯を残した）。

### 困ったこと

**作った hook は、このセッションでは効かなかった。**
#151 をマージしても知らせが出ない。**手で叩けば動く**ので、中身は正しい。
**Claude Code が設定をセッションの頭で読むため**と見ているが、**確かめていない。**
次のセッションで実際にマージしたときに出るかどうかが確かめ所。申し送りに入れた。

## #153 #154 起票、#155 正本に `timeline.yml` を書く

**決めたのに記録に無いものを、まとめて片付けた。**

### #153 エコー区間を生音基準に直す（起票）

**今日「編集点は生音の時刻で持つ」と決めたのに、エコーの実装は `trimmed.wav` 基準のまま。**
決めたことと実装が食い違っている状態を、Issue にした。

直す対象は7か所（`echo_positions` / `normalize_echoes` / `clean.json` / 波形 / `_why_stale` / 試聴 / 文書）。
**#85 の実装と一緒にやる**と書いた。単独で先にやると、画面が中途半端に2つの物差しを持つ期間ができる。

**データの移行は要らない**（`ep01` の `echoes` は空）。

### #154 音源が2本あって `timeline.yml` が無いと、黙って1本目だけ使う（起票）

**前からの挙動。2本が現実になった今日から罠になった。**

GUI からは起きない（`_clear_raw` が1本に保つ）が、**コマンドと手置きでは起きる。**
そして **#85 で `_clear_raw` を外すと、GUI 側にも開く。**

**「名前順に繋ぐ」案は採らないほうがよい**と書いた。**意図しない順で繋がったときに気づけない**ため。
今日ずっと避けてきた「黙って壊れる」形。

### #155 正本に `timeline.yml` を書く

- **きっかけ**: `scope-reviewer` が #145 のレビューで「**`docs/features.md` にも `docs/components.md` にも
  `timeline.yml` が一行も無い。grep で 0 件**」と指摘していた。確かめたら、そのとおりだった
- **4か所に書いた**（`docs/features.md` / `docs/components.md` / `README.md` / `CLAUDE.md`）。
  **1か所だけ直すと、残りが古いことを言い続ける**（この日2回やった）
- **`CLAUDE.md` は「短く保つ」決まりなので1行だけ**にして、詳しくは `docs/features.md` に送った
- **「まだできないこと」も並べた**（編集点を当てられない / 画面から2本目を置けない / エコーは古い基準のまま）。
  **書けることだけ書くと、できるように読まれる**

### 学んだこと

**「決めたことは、その場で正本に書く」は、Issue だけでは足りない。**

この日、同じ形を**3回**やった。

| | 決めた所 | 直していなかった正本 |
|---|---|---|
| 1 | #79 のコメント（`cut` を GUI に出す） | `docs/features.md` の決定3 → #146 で直した |
| 2 | #79 のコメント（生音基準にする） | エコーの実装そのもの → #153 に起票 |
| 3 | #144（`timeline.yml` を作る） | `docs/features.md` / `docs/components.md` → #155 で直した |

**Issue は決めた経緯、`docs/` は決まった結果。** 役割が違うので、片方では足りない。

## #157 音源が2本あって並びが無ければ断り、止まった理由をログに残す（#154 を閉じた）

- **決めたこと（ユーザー）**: **案A（断る）。** 2本置いた人は2本使うつもりなので、
  1本で進んだ結果はほぼ確実に間違っている
- **足された要求**: 「**止まった際になぜ止まったか後で追えるようにしましょう**」

### 止まった理由が、あとから追えるようになった（全工程）

**いままで、工程が例外で落ちると `00_logs/<工程>.log` には ffmpeg の出力までしか入らなかった。**
理由は端末のトレースバックにしか出ず、**あとから追えなかった。**

`main()` で理由をログに書いてから投げ直すようにした。**カットでも文字起こしでも動画化でも効く。**

### 自分で見つけた落とし穴

**`ep01/00_raw` には「録画.mp4」と「録画.track0.wav」の2つがある。**
後者は **`find_raw` 自身が録画から取り出したもの。**

**素朴に「2つあったら断る」と書くと、OBS の録画がある回が全部止まる。**
**9/24 のリハーサルが動かなくなるところだった。**

`source_candidates()` で「人が置いた音源」だけを数えるようにし、実物の `ep01` で確かめた。

### レビューで見つかった、同じ種類の穴

**`glob("*.wav")` は `second.WAV` を拾わない**（Linux は大文字小文字を区別する）。
録画側は `suffix.lower()` で吸収しているのに、**音声側だけ吸収していなかった。**

**この PR が防ごうとしている事故が、拡張子が大文字なだけで起きていた。**

`find_raw` の最後の拾い方も同じ書き方だったので、**そちらも直した。**
片方だけ直すと「**数えるときは見えるのに、使うときは見つからない**」というちぐはぐが残る。

### 「ついでの改修」の範囲が、はっきりした

上の直しを「`CLAUDE.md` の『ついでの改修はしない』に触れる判断だが、
**同じ関数の同じ欠陥で、半分だけ直すほうが悪い**と考えた」と断って出したところ、
ユーザーから**その考えでよい**と明示された。

> **ついでの改修というのは、プラスアルファの機能の実装や、別の修正をついでにやることです。**

- **同じ欠陥が同じ場所にあるなら、一緒に直す。** 断りも要らない
- **別の機能を足す・別件のバグを直す**のは、指示が無ければやらない（報告するか Issue にする）

### その他に決めたこと

**#137 のクレジットは毎回付ける**（ユーザーの判断）。合成音声を使ったかで判定しない。
**判定が要ると、判定を間違えた回だけクレジットが落ち、規約の義務なのに気づけない。**

**切り抜き（#91）にも付ける。** 番組側が「判断していない」と書いていた分を、ユーザーが判断した。
実装のとき、**通しと切り抜きが同じ1か所を通るようにする**（道が分かれると、片方だけ落ちても気づけない）。

## #160 概要欄に VOICEVOX のクレジットを必ず入れる（#137 を閉じた）

**決めた直後に実装した回。** 未決が残っていなかったので、判断で止まらずに通せた。

- **毎回付ける。** 「この回は合成音声を使ったか」で判定しない。
  **判定が要ると、判定を間違えた回だけ落ちる**。規約の義務なので、落ちても気づけない形にしない
- **並びは 本文 → クレジット → 目次**
- **手順書も直した。** 今日の午後「手で足す」と書いたので、**そのままだと 9/24 に二重貼り**になる

### レビューを3人呼んだ。3人とも別の所を見つけた

ユーザーの問い「**GUI とかは大丈夫ですか？**」から `design-reviewer` を、
続けて「**であればユーザー担当も呼びましょう**」から `user-reviewer` を呼んだ。

| 担当 | 見つけたもの |
|---|---|
| **`code-reviewer`** | **本文で VOICEVOX に触れただけで、クレジット行が入らない**（部分一致で見ていた。**番組が VOICEVOX を扱う回は普通にある**）／`grep` で呼び出しを数えるテストが、コメント1行で壊れる |
| **`user-reviewer`** | **古い形式の回で、並びが「本文 → 目次 → クレジット」に崩れる**／**書き方のゆれで二重になる**（`voicevox:` / 全角コロン / コロンの後の空白） |
| **`design-reviewer`** | **コピー欄のプレビューに、クレジットの有無が出ない**（入っていてもいなくても見え方が同じ。**落ちていても気づけない**）／文言3か所が「章」しか言わない |

**1人だけなら、たぶん半分しか見つかっていない。**
そして**3人とも「自分の持ち場」を見て、別のものを拾った**。担当を分けている意味がここに出た。

### 直したこと

**処理側**: 目次の前に入れる／行がまるごと一致するかで見る／大文字小文字・コロン・空白を吸収する／
`grep` で数えるテストをやめ、**両方の入口を実際に呼んで出力を突き合わせる**形にした。

**画面側**: コピー欄に「**…＋クレジット・目次1件**」と出し、**無ければ「（クレジットなし）」**を出す。
API が「入っているクレジット」を返すようにした。文言3か所を直した。
見出しは「**概要欄（章・クレジットつき）**」（ユーザーの判断）。
**実画面の幅 1280px / 1500px で、折り返さないことを確かめた**（3つとも高さ 68px）。

### この日の失敗（もう1つ）

**申し送りを直すとき、置換の文字列に打ち間違いを入れて本文を壊した。**
「通しリハーサル」が「通しリ**hal**ーサル」になった。

しかも**それを直そうと同じ置換を何度も重ねて書き、全部空振りしていた**
（元の文字列が違うので当たらない）。**雑に重ねた時点で気づくべきだった。**

**確かめて見つけた。** 直した後に `grep` で「通しリ....サル」を見て、カタカナ以外が
混ざっていないことを確認している。**書き換えたら、書き換えた所を見る。**

**そしてもう1つ。この議事録の冒頭の数字を、測ったのに使わずに書いた。**
`day-stats.sh` は「PR 20件・文書796行・コード1426行」と出したのに、
**22件・887行・1739行と書いた。** 測った値が目の前にあるのに、そこから写さなかった。

**今日この節を5回書き直していて、毎回「測り直す。写さない」と書いていた。**
**書いている本人が、測った値を使っていなかった。**
直すときは、**スクリプトの出力をそのまま流し込む形**にした（手で打たない）。

## この日に確かめられたこと（申し送りから消せるもの）

- **1回分を通しで作れるか** — **作れた。** ただし**約57秒・1コーナーの試験音**での話で、
  **30分・複数コーナーの本番（#53）はまだ**
- **`docs/rehearsal.md` が当日役に立つか** — **手順としては通った。** 食い違い2件を直した。
  ただし**人が読みながら操作したわけではない**（機械で画面を操作した）
- **#130 で足した裏取りの要求が、レビューの質を変えるか** — **変えた。**
  両担当とも「確かめられなかったこと」を分けて出し、**裏を取れた指摘だけを🟡にしていた**
- **30分・複数コーナーの回で章が並ぶか** — **まだ。** コーナー7つの入力までは確かめたが、通してはいない

## 測った時間（約57秒の音・1コーナー）

音源 3秒 / 下見 15秒 / 整音 3秒 / 文字起こし 15秒 / メタデータ 18秒 / 動画 3秒。

**手順書には書かなかった。** 1分の音の数字を 30分の見当に使われると困る（手順書自身がそう断っている）。

## 次にやること

**方針が変わったので、朝と違う。**

1. **#79 の論点1 の残り**（時刻の書き方・要素の型の分け方）。ここが決まると #88 #89 が動ける
2. **#85 の実装**（タイムライン編集）。#86 #87 が閉じて、前提は立った
3. **#53 通しリハーサル**（ユーザー・9/24）。**手順書は直した**（#136 / #140）。
   **ただし、この後の実装が入った状態で通すことになる**
4. **#142 は単独で直さない**（`timeline.yml` のときにまとめて消す）
5. **#135・#137・#138 は着手しない**
