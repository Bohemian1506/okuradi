# Day 8（2026-09-25）作業の画面を作り、ラベルを決め、F12 で `/始め` が走る形にし、第0回を `ep00` に決めた

**PR 7件・文書168行増17行減・それ以外842行増25行減。`pytest` 383 → 425。**

朝の一手は、前の晩に決めた **#183**。番組側からの返事は無し（#53 #85 #183、r-hoso）。

**自動モードに、自分の設定を変える作業を止められた。** 書き込みだけでなく、テストを読むコマンドまで止まった。
**ユーザーの判断で、この作業の間だけ自動モードを外した。**

夕方、**ユーザーの提案で Issue のラベルを整理した**（#196）。**2つのリポジトリをまたぐやりとりは初めてで、想像がつかない**という相談から、ラベルを3つ決めた。

作っている途中で、**day-7 の #181（`/始め` を自動で始める）は「打たなくても始まる」わけではない**と分かった。**夜、#194 で `start.sh` が最初の一言として `/始め` を渡す形にした**（#198）。

---

## #193 feat: 作業の画面を整える（`/始め` でペインを作り、`/作業終了` で閉じる）

### きっかけ
#183。r-hoso 側と同じように画面を割り、作業中に見たいもの（変更の差分・GUI のログ・今日の一手）を並べる。
#180 のレビューで出た「**続きから（`start.sh -c`）開いた朝は `/始め` が促されない**」も、ここで塞ぐ。

### 決めたこと
Issue に「決まっていない」とあった4つ。

| | 決め方 | 中身 |
|---|---|---|
| **GUI のサーバーを `/始め` で毎回立てるか** | **ユーザーの判断** | **立てない。使うときに、ログのペインの中で立てる**。理由: 立てない日もある / `--host 0.0.0.0` は同じ LAN から見える |
| 印の置き場所 | 仮置き | `.claude/state/`（git の管理外） |
| 今日の一手のペイン | 仮置き | `/始め` が `.claude/state/今日の一手.md` に書き、`less` で開く |
| ペインを作るのは `/始め` の最初か最後か | 仮置き | 最後（今日の一手が決まってから映す） |

Issue の本文から、形を3つ変えた（Issue にコメント済み）。

| 本文 | 実装 | 理由 |
|---|---|---|
| 差分は `herdr pane run` で流す | **hook はファイルに書くだけ。ペインの中の係が映し直す** | シェルに毎回コマンドを打ち込まずに済む。hook が herdr に頼らない |
| `/始め` が印を消す | **起動の hook と `/始め` の両方が消す** | hook だけだと、同じセッションのまま `/始め` を打った朝に残る（レビュー） |
| `exit` | **Claude は「`/exit` で終わります」と出す** | Claude は `/exit` を自分では打てない |

**認証情報のファイルの差分も画面に出す**（ユーザーの判断: 「私しか見ないので GitHub に上がってなければ OK」）。

### やったこと
- `.claude/scripts/作業ペイン.sh`（`open` / `gui` / `close`）。ペインは**名前で探す**
- `.claude/hooks/show-diff.py`（Edit / Write のたびに差分を書く）と `.claude/scripts/変更を映す.sh`（**最初は空**）
- `/作業終了` を新設。**区切っていなければ先に区切るかを聞く** → 印 → ペインを閉じる
- 起動の hook は、**印があれば続きから（resume）でも `/始め` を促す**
- **実際の Herdr で確かめた**: 高さ 22 / 14 / 12。閉じる → 作り直すの1往復。本物の Edit で差分が映った（`@@ -37,7` = Claude Code が渡す `structuredPatch` を使えている）。別のタブの r-hoso のペインには触らない

### レビュー（code / scope / user）
🚨 は2件。どちらも直した。

- **印を消すのに失敗すると、起動の促しまで丸ごと消える**（code・隔離した場所で再現）。**後片付けが本体を道連れにしていた。** 促しを先に出す形にした
- **GUI で工程が動いている最中に `/作業終了` を打つと、確認なしでログのペインを閉じる**（user）。
  **コードで確かめると半分正しかった。** 工程は別のプロセスグループ（`web/runner.py:145`）なので取り残され、作りかけのファイルを消す後片付けも走らない。
  「詰まって止まる」は違う（読み手が消えるので、書いた時点でエラーで落ちるはず）。**GUIログで何か動いていたら、どれも閉じない**形にした

ほかに、herdr が失敗したら「ペインが無い」と区別がつかず二重に作る穴（code）、`/始め` でも印を消す（scope）、文書の古い記述（scope）を直した。
**足したテスト5件は、直す前の版で5件とも落ちることを確かめた**（緩いテストでないことの確認）。

### 困ったこと / 学んだこと
- **自動モードは、自分の設定（`.claude/` の下）を変える作業を止める。読み取りまで止まる**（day-7 に続いて2回目）。
  仮の hook で入力の中身を書き出そうとしたのも止められた。**回り道はせず、ユーザーに進め方を聞いた**
- **day-7 の #181 は「起動しただけで始まる」と書いたが、正しくない。** hook は Claude に知らせを渡すだけで、**Claude は人の最初の一言を待つ。**
  今朝も知らせは届いていたが、動き出したのはユーザーが `/始め` を打ったとき。**「作った」と「届いた」の次に、「動き出す」があった**
- **今朝の Claude は `start.sh` を通っていなかった**（`lead` という名前が無い）。F12 のどの項目から入ったかは分かっていない
- **未確認**: `/作業終了` → 続きから開いて促されるか / `作業ペイン.sh gui` でサーバーが立つか

## #196 chore: ラベルを決める（道具・番組側から・番組側の判断待ち）

### きっかけ
ユーザー: 「182ってすぐできます？ISSUE回り整理したほうがいい気がするんですが」。
#182 は day-7 に立てたもの。朝の状態出しは「ラベル無しは番組側かも」と添えるが、**こちらもラベル無しで立てていた**（#180 #183 #194）。

### 検討したこと
- **立てる人は、番組側もこちらも同じアカウント**（Bohemian1506）。作者では見分けられず、**ラベルだけが手がかり**
- #182 の3案: A `enhancement` に寄せる / **B 道具用のラベルを作る** / C 決まりだけ。
  **A は `CLAUDE.md` の「あとで良くすること」とずれる**（#183 #194 はすぐやる）。**ユーザーは B**
- ユーザー: 「柔軟的にやるために他に作ったほうがいいラベルとかありますかね？二つのリポジトリまたいでやり取りするというのが初の試みなので、ちょっとその辺想像ができなく」
  - **`番組側から`**: 「ラベルが無い＝番組側」は**無いことで見分ける**ので、付け忘れ1つで崩れる。有ることで見分ける形に
  - **`番組側の判断待ち`**: **すでにあったが、一度も付けていなかった**（0件）。**r-hoso の `/始め` は毎朝これを見に来る作り**なのに、こちらはコメントに書くだけで、向こうの朝の点検に載っていなかった
  - 作らなかった: r-hoso 側の「アプリ側からのお願い」ラベル（これまで2件・題名で分かる。r-hoso の中のこと）

### 決めたこと
**3つとも使う**（ユーザー: 「Bでいきましょう」）。**ラベル無しは付け忘れ**になる。

### やったこと
- ラベルを作って付けた（`道具` → #180 #182 #183 #184 #188 #194 / `番組側から` → #127）
- 状態出しの注意書きを「ラベル無しは付け忘れ」に。`番組側から` の Issue は名前まで出す
- `CLAUDE.md` のラベルの行に3つ足した
- **r-hoso のセッションにメッセージで知らせた**（ユーザー: 「すでに r-hoso もセッション動いているのでメッセージ送ってくれれば OK」）。
  `番組側から` を付けてもらう。**r-hoso の `CLAUDE.md` を直すかは、向こうでユーザーが決める**（r-hoso の決まり「セッション同士で決めない」）

### 困ったこと / 学んだこと
- **閉じた Issue にラベルを付けると「最近動いた」に上がる。** 次の朝の状態出しに並ぶ

## #198 feat: start.sh が最初の一言として /始め を渡す

### きっかけ
ユーザー: 「今って F12 を押して Herdr を起動するので start.sh 打たずに直で Claude 起動してます。で、私が手動で /始め をうちました。F12 でこの画面出したときに自動で start.sh 走らせることってできます？」

### 調べたこと（**最初の見立ては間違っていた**）
- はじめ「`lead` という名前が無いので `start.sh` を通っていない」と答えた。**Herdr のログとプロセスをたどると、通っていた**
  - 22:20:08 `start.sh` が `lead` を起動 → **22:20:24 Claude Code 2.1.282 がインストール** → 22:20:32 `lead` が終わる（会話の記録は0行）→ 22:20:35 同じペインで `claude` が打ち直された（いまのセッション）
  - ユーザー: 「何かやった覚えはないんですよね」
  - **落ちた原因は分からない。** 更新と同じ時刻だが、前日の更新では落ちていない。観測1回
- **Claude は人の最初の一言を待つ。** 起動の hook（#181）は知らせを渡すだけ。今朝も知らせは届いていたが、動き出したのはユーザーが打ったとき

### 決めたこと
- **案のとおり作り、落ちた件は様子を見る**（ユーザー: 「OK」。原因を追うには次の更新を待つしかないため）
- 仮置き: 続きから（`-c`）は `/作業終了` の印があるときだけ渡す / 起動の hook は `claude` を手で打ったときの保険として残す

### やったこと
- `start.sh` に `lead_args`。新しく起動 → `/始め`、続きから → `--continue`（印があれば `/始め` も）
- **本物で確かめた**: `herdr agent start test194 ... -- "/cost"` で、起動の引数のスラッシュコマンドが走った（`/始め` は今日の一手を上書きするので、害の無い `/cost` で）
- **作っていて #193 が生んだ欠陥を見つけた**: `start.sh` は `lead` を置く先に「空いているシェル」を探すが、**GUIログのペインもそう見える**。名前の付いたペインを外した

### レビュー（code）
🚨 無し。**文書で「何も打たずに始まる」と言い切っていたのを「未確認」に直した**（PR 本文では未確認と書いていた）。
渡る直前の引数を start.sh の実物の行で通すテストを足した（わざと壊すと落ちる。ただし落ちた理由は「行が見つからない」で、中身のずれを見分けたわけではない）。

### 困ったこと / 学んだこと
- **名前が無いことを根拠に「通っていない」と言った。** 名前は打ち直すと消える。**ログで時刻をたどれば分かった**
- **新しい画面（#193）を作ったら、既存の道具（`start.sh`）が拾う対象が変わった。** 「空いているシェル」の意味が変わっていた
- **未確認**: F12 から通しで何も打たずに走るか / `--continue` と `/始め` を一緒に渡す形

## #200 feat: 第0回（ep00）を GUI から作れるようにする

### きっかけ
ユーザー: 「細かい作業をやりましょう。ちょっと85はしんどいです」。細かい候補を4つ出し、**1（第0回のフォルダ名）・2（残ったブランチの掃除）・3（`.wslconfig` の古い記述）**に決まった。

### 検討したこと（**一度、間違った根拠で勧めた**）
- はじめ「第0回の中身は `ep01` にある」として **`ep01` → `ep00` の名前変更**を勧め、ユーザーは A（それ）を選んだ
- 直す所を探していて `docs/rehearsal.md` の「**`ep01` は試験用。上書きしない**」に当たり、実物を見た。**`ep01` は 9/14 の約1分の試験音・コーナー1つ。第0回の中身は r-hoso 側の構成・台本だった**
- **変える前に止めて、案を出し直した**: P GUI で 0 を作れるようにし、当日 GUI から `ep00` を作る / Q 試験用の `ep01` を別名に移す / R 何も変えず `ep02` で作る（番号がずれる）

### 決めたこと
- **P**（ユーザー: 「Pde」）。**リハーサル（#53）は回を作るところから GUI で通すもの**なので、当日 GUI から作るのが本来の形
- **試験用の `ep01` と本収録の第1回がぶつかる件は、本収録の日付が決まってから**
- `.wslconfig`: **`networkingMode=mirrored` の行を消す**（ユーザー: 「どちらもAで」）。Windows 10 では効いておらず、失うものが無い。`wsl --shutdown` は打たず、次の再起動で効かせる
- r-hoso のセッションに、決まったこととして知らせた（番組側から「アプリ側で決めて」と投げられていた件）

### やったこと
- 回の番号の下限を 1 → 0。**空欄のまま作成したら止める**（`Number("")` が 0 になり、黙って `ep00` ができるため。下限を下げたことで生まれる穴）
- **ヘッドレスの Chrome で「新しい回」を操作した**（サーバーは `127.0.0.1` で一時的に立てて止めた。本物の回ができないよう、送る直前で横取りした）
- GitHub に残っていたマージ済みのブランチ9件を消した
- `.wslconfig` の行を消した（控えを取ってから）

### レビュー（code）
**指摘なし。** 空白だけ・`e` だけの入力も、本物の入力欄で空文字になり止まることを確かめてくれた。

### 困ったこと / 学んだこと
- **okuradi の `ep01` と r-hoso の ep01 を混ぜて読んだ。3回目**（day-6 に2回）。申し送りの「気をつけること」に書いてあった。
  **「中身はどこにあるか」を言う前に、そのフォルダの `config.yml` と `00_raw` を見る**
- **下限を下げると、これまで下限が弾いていた入力（空欄）が通るようになる**

## 次にやること
- **次に F12 で開いたとき、何も打たずに `/始め` が走るかを見る**（#194 の未確認）
- **#85**（タイムライン編集）。第0回（10月頭）で通すのに要る
