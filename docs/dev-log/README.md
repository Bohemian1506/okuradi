# 開発記録（議事録）

PR ごとに「なぜその PR に至ったか」の経緯を、マージ直後の新鮮なうちに残す。後から「なぜこうしたか」を見返すための記録。

## ファイルの付け方
- `day-N.md`。N は **作業した日の連番**（カレンダーの日付ではない。作業しなかった日は数えない）
- 同じ日に PR が複数あれば、その日の `day-N.md` に追記していく
- day-1 = 2026-09-14

## いつ・どう書くか
1. **PR をマージしたら、すぐに書く**（次の作業に入る前）
2. `docs/day-N-<PR番号>` のようなブランチを作り、`day-N.md` に追記する（無ければ作る）
3. `docs: day-N 議事録（#PR番号）` という PR にしてマージする
- 議事録だけの PR には、レビュー担当は呼ばなくてよい
- 誤字直しなど、経緯を残すほどでない PR は省いてよい

## 書くこと
日の冒頭に、その日の要点を数行で書く。PR ごとに「経緯」の節を足していく。

### 冒頭のまとめは、区切るたびに測り直す
**前の版から写さない。** day-4 に、朝書いた「実装らしい実装はしていない日」をそのまま残し、
夕方までに729行書いたのに直さなかった。

```
docs/dev-log/day-stats.sh          # 今日
docs/dev-log/day-stats.sh 2026-09-20
```

マージした PR の数、**文書とそれ以外に分けた行数**、新しく足したファイルが出る。
**その数字を冒頭に書く。**
書くことがない見出しは省いてよい。

```
# Day N（YYYY-MM-DD）<その日の要約>

<その日の要点を数行>

## #<PR番号> <PR のタイトル>
- きっかけ: 何があってこの作業を始めたか
- 検討したこと: 出た案と、選ばなかった理由
- 決めたこと: 決定と理由（ユーザーの判断は、その言葉に近い形で残す）
- やったこと: 変えたもの、閉じた・作った Issue
- 困ったこと / 学んだこと

## 次にやること
```

## 一覧
| 日 | 日付 | 要点 |
|---|---|---|
| [day-1](day-1.md) | 2026-09-14 | パイプラインの取り込みと、GUI アプリ化の機能決め |
| [day-2](day-2.md) | 2026-09-17 | 部品の洗い出しと、担当・Herdr・議事録・起動コマンドの体制づくり |
| [day-3](day-3.md) | 2026-09-19 | 技術構成とデザインを決め、GUI を最後まで実装した（#5〜#8）。波形を wavesurfer.js に（#36）、見た目の作り込みも片付けた（#42 / #59〜#61 / #64 / #65 / #69）。**残りは通しリハーサル #53 だけ** |
| [day-4](day-4.md) | 2026-09-20 | **PR 27件・コード729行/文書799行。** #77 を片付け、**#86 を決めて閉じた**（案E' + クリップの最低幅）。番組側が立てた #96 を実装。**自分が作ったフックの穴をレビューで指摘され、テストで守る形に直した**（#107 / #110 / #112）。道具を4つ足した（`/区切り`・始まるときの状態出し・許可リスト・裏走り）。30分番組の実装には着手しない |
| [day-5](day-5.md) | 2026-09-21 | **PR 6件・コード250行/文書399行。`pytest` 246件。** `/区切り` の対になる `/始め` を足し（#124）、**#53 のリハーサル手順を書いた**（#126）。**番組側の決定が2つ届いた** — コーナーの尺は可変（#127・閉じて #79 へ）と、**章はコーナーと1対1**（#106・同じ日に訂正あり）。**その日のうちに #106 を実装し**（#129）、**議事録から失敗パターンを数えて守りを3つ足した**（#130・レビューの裏取り要求／件数を機械が数える／API キーと認証情報のテスト） |
| [day-6](day-6.md) | 2026-09-22 | **PR 32件・文書923行/コード2015行。`pytest` 246→366。** 朝は **通しリハーサルを空打ちして手順書を直した**（#136）。午後、**番組側の決定で方針が変わり**（「今さら聞けない」がずんだもんの合成音声丸ごと）、**「レーンまで作る。9/24 への遅れは許容」**に。**#87 を閉じ**、**#79 の論点1 を決め**（`timeline.yml`・**時刻を2つに分ける Premiere 式**）、**タイムラインを実装**（#145）→ **決定3を取り下げ**（#146）→ **音源2本を繋ぎ**（#147・**実音源で通したのはこの日がはじめて**）→ **並びが無ければ断り**（#157）→ **クレジットを自動で入れ**（#160・**レビュー3人が別々の穴を見つけた**）→ **朝の事故を夜に直した**（#162）。**自分の失敗を仕組みにした日**（#151） |
