# 申し送り

作業を区切るときに、次の人（次のセッションの Claude）へ残すメモ。**最新の1回分だけを書き、区切るたびに上書きする。** 経緯は `docs/dev-log/` を見る。

**main の位置に、コミットの番号や PR 番号を書かない。** この申し送りはその PR の中で書くので、
番号を書くとマージした瞬間に1つ古くなる。直してもその直しがまた main を進めるので、永久に追いつかない。
位置が要るときは `git log --oneline -1` を打つ。ここに書くのは**番号が変わっても正しいこと**だけにする。

## 2026-09-20（day-4 の終わり・2回目の区切り）

### いまの状態
- main は、**この申し送りを載せた PR をマージした所**（位置は `git log --oneline -1`）。**開いている PR は無し。ローカルのブランチは `main` だけ**
- **必須の17機能と、部品1〜19 の実装がすべて終わっている**（`docs/features.md` の「実装の状況」）。`pytest` 188件が通る
- **この日は実装をしていない。** 午前は #77（GUI の開き方）、午後は30分番組化の設計、夕方は #86 の実機確認と倍率の測定
- **`ep01/config.yml` に、コミットしていない変更が1つある。** ユーザーが GUI で保存したエコー区間（`10.69〜14.01 / light`）。消さずに残してある。要らなければ `git checkout ep01/config.yml`
- **番組のリポジトリは別。** 番組の仕様・構成・台本・収録メモは [r-hoso](https://github.com/Bohemian1506/r-hoso)（非公開、`~/workspace/r-hoso`）。okuradi はツールだけ
- 波形は wavesurfer.js 7.8.6（#36）。`web/static/vendor/` に置いてあり、CDN からは読まない
- **`pytest` は 222件**（もとの188 + フックのテスト34）。`tests/test_hooks.py` が `.claude/hooks/` の2つを守っている
- **区切るときは `/区切り`**（`.claude/commands/区切り.md`）。間違えやすい所を集めてある
- 回のデータは `ep01` だけ。音声と途中のファイルは git の管理外

### 開いている Issue（16件）
| Issue | 内容 | いま |
|---|---|---|
| #53 | 新しい GUI だけで1回分を通し、`app.py` を消す | **ユーザーが実物の音声でやる**（Claude にはできない）。**v1 の最後の関門** |
| #106 | 30分番組化で見直す「10分」前提の一覧 | **文言の分は #111 で済んだ。** 残るのは本体（`build.py:514` の章の数 3〜6個）で、**番組側の決定待ち** |
| #112 | フックが CRLF のコマンドを素通りさせる | 小さい。直すときは `tests/test_hooks.py` に**落ちるテストを足してから** |
| #79 #82 #85 #87〜#93 | 30分番組化の設計 | 全部「段取り」。着手していない。地図は #85 |
| #97 #98 #99 | 倍率まわり（測定・時刻を保つ・操作を作る） | #97 は測定済み。#98 #99 は**前提が立っていないので着手しない** |

**#86 と #96 は閉じた。** #86 の決定は下記。#96（`series_rules` を30分番組のコーナー名に揃える）は
#105 で実装し、実際の音声で meta 工程を通して確かめた。

### #86 は決まった（閉じた）
**レーンは3行。種類ごとに、時間的に重ならないものを1行へ重ねる。**

| 行 | 中身 | 上限 |
|---|---|---|
| 1 | 本編（トーク8 + OP / ED / ジングル） | 17本 |
| 2 | BGM（1コーナー1曲） | 8本 |
| 3 | **SE・合いの手** | **決めなくてよい** |

行3 は **marker ではなく本物の音源**を重ね、クリップに CSS で最低幅（`min-width: 14px`）を与える。
これは見た目ではなく、**掴めるようにするための決定**（無いと全体表示で 0.28px になる）。

marker を落としたのは、**音を持たないので試聴で鳴らない**ため。最低幅を使えば音源のまま
「どの倍率でも見える・掴める」が成り立つので、marker を選ぶ理由が残らなかった。

**決定の全文と、落とした案A〜E の理由は #86 のコメントにある。** 測定は #97。

**実装するときの注意も #86 に引き継いである**（重なりを見せる必要 / 尺の外へ出せない /
最低幅14pxは仮の数字 / ジングル10秒は未確認 / 細い線として見せる）。
**実装で何か出てきたら別の Issue にする**（ユーザーの判断）。

### 次にやること
1. **#53 通しリハーサル**（ユーザー）— 実際の収録で最初から最後まで通して、詰まった所を Issue にする
   - 開き方は下の「GUI の起動」
   - 通せたら消すもの: `app.py` / `requirements.txt` の streamlit・altair・pandas・**numpy** / `CLAUDE.md` の「古い GUI」の行 / `README.md` の Streamlit の節と「まだ1回分を通していない」の断り
   - **通せたら見直すもの**: `README.md` の断りの最後の一文（「#53（通しリハーサル）を通してから着手する」）。
     **通した瞬間に古くなる。** 断り自体（収録1本の前提）は mix が入るまで残す
2. 通して詰まった所が出たら、Issue にしてから直す。**Claude ができるのはここから**
3. **設計の Issue には着手しない。** #53 を通してから。30分の形に手を入れてから壊れると、切り分けができなくなる

### 番組側（r-hoso）とのやりとり
- 案E の検証結果を `r-hoso-00`（r-hoso 側の Claude）に Herdr のメッセージで送った。**届いて読まれた**
- **返事は #86 のコメントで来る。** r-hoso の `CLAUDE.md` が「番組側で決めたことがアプリに関わるときは、
  okuradi の Issue に書きに行く」と決めているため。**メッセージの返信としては返ってこない**。
  送ったあとは、**メッセージではなく Issue を見直す**
- 向き: **番組の決定がアプリの作りを決める。逆ではない。** 番組側で決めたことは okuradi の Issue に書きに来る
- r-hoso は**別の `CLAUDE.md`** を読む。okuradi のセッションから r-hoso を直接いじらない

### 作業の始め方
- GUI の開き方は下の「GUI の起動」（**ここに書くと2か所になって片方が古くなる**）
- コマンド: `.venv/bin/python build.py ep01 --from clean --to clean`（GUI が無くても番組を作れる状態は保つ）
- 追加インストールは `uv pip install --python .venv/bin/python <名前>`（この `.venv` は uv が作ったもので、pip が入っていない）
- WezTerm の F12 から開く（使い方は `docs/herdr.md`）
  - 「herdr: okuradi」: okuradi だけ
  - 「herdr: フリーライフ資料」: okuradi の `lead` も起動してから、フリーライフを開く
  - 「herdr: okuradi + フリーライフ」: 両方を起動して、okuradi を開く
  - **「herdr: okuradi + アール放送局」**: 両方の画面を用意して、okuradi を開く（アール放送局は画面だけ）
  - **「herdr: 全部（フリーライフ + okuradi + アール放送局）」**: 3つとも
- 前の会話の続きから始めるときは、端末で `~/workspace/okuradi/start.sh -c`
- 作業のルールは `CLAUDE.md`（Issue から始める / 勝手に決めない / レビュー担当は必要なときに呼ぶ / マージしたらすぐ議事録の PR）

### GUI の起動
```
.venv/bin/python -m uvicorn web.main:app --host 0.0.0.0 --reload   # 起動
./open-gui.sh                                                       # ブラウザで開く
```
- **`--host 0.0.0.0` が要る。** 付けないと WSL の中だけで待ち、Windows 側のブラウザから入れない
- **`http://localhost:8000` は通らない**（Windows 10 では WSL の localhost 転送が効かない。#77 で確かめた）
- `./open-gui.sh` が、そのときの WSL の IP を調べてブラウザを開く。**IP は再起動すると変わる**ので、数字を文書に書かないこと
- サーバーが動いていない / `127.0.0.1` だけで待っているときは、`open-gui.sh` が開かずに理由を出して止まる
- `--host 0.0.0.0` は同じ LAN の他の機械からも見える。外のネットワークでは避ける

### 画面の確かめ方（CDP）
ヘッドレスの Chrome を CDP で動かすと、**実際にマウスを動かしてドラッグまで試せる**。#86 / #97 はこれで測った。

- ブラウザは `~/.cache/ms-playwright/chromium-*/chrome-linux*/chrome`。**版は勝手に入れ替わる**ので `ls` で探す
- 起動: `--headless=new --remote-debugging-port=9222 --no-sandbox --user-data-dir=<作業場所>`
- **`pkill -f "remote-debugging-port=9222"` を使わない。** パターンが自分のコマンド行にも当たってシェルごと死ぬ。`ps` で PID を拾って `kill` する
- 測るときは `python3 -m http.server` で配る。**キャッシュが効くので、冷えた状態を測るなら URL に `?v=<毎回違う値>` を付ける**

### まだ確かめていないこと
- **1回分を通しで作れるか（#53）。** 工程ごとには `ep01` で動かして確かめたが、収録から公開用のコピーまで一続きに通してはいない
- **実際の収録音でタイムラインがどう見えるか。** #86 / #97 の試験はサイン波なので、波形が帯にしか見えていない
- **拡大・縮小を繰り返したときの操作感。** 1回ずつしか測っていない
- **土台トラック（全尺ぶんの無音）を軽くできるか。** 圧縮形式にすれば復号が速くなる可能性があるが未検証
- **`.wslconfig` の `networkingMode=mirrored` を消すと `localhost` が通るようになるか。** 確かめるには `wsl --shutdown` が要るので、作業中はできない（#77 に記録）
- PC を再起動した後、「herdr: okuradi + フリーライフ」で、止まった状態から両方が起動するか
- Herdr で Issue を並行で進める流れ（`herdr worktree create` → 実装担当の起動 → 片付け）

### リポジトリの外にある設定（消えたら作り直す）

| 設定 | 場所 | 作り直し方 |
|---|---|---|
| `.wslconfig`（Windows 側） | `C:\Users\hiros\.wslconfig` に `networkingMode=mirrored` と書いてあるが、**Windows 10 では効かない**（mirrored は Windows 11 22H2 以降）。消すか注記するかは #77 で決める |
| F12 のメニュー（**5つ**） | `C:\Users\hiros\.wezterm.lua` の `launch_menu` | 書き方は `docs/dev-log/day-2.md` の #13 と `docs/herdr.md` |
| Herdr の Claude 連携 | `~/.claude/hooks/herdr-agent-state.sh` など | `herdr integration install claude` |
| Claude が Herdr を操作するスキル | `~/.claude/skills/herdr/SKILL.md` | `herdr --skill > ~/.claude/skills/herdr/SKILL.md`（Herdr を更新したときも） |
| Claude Design のプロジェクト | `共通の枠を出しました`（claude.ai/design）。**見本はここが正本**。実装が見本を追い越したら、`github.md` の同期メモと一緒に直す（#65・#70 でやった）。**直したら `docs/design/` にも取り込み直す**（`serve_url` に `&raw=1` を付けると元のファイルが取れる）。どちらの見本を見るかは `docs/design/README.md` の表 |
| Claude Design とつなぐ MCP | `~/.claude.json`（`-s user` で入れた。公開リポジトリなので `.mcp.json` は作らない） | `claude mcp add -s user --transport http claude_design https://api.anthropic.com/v1/design/mcp` → `/design-login` → **Claude を立ち上げ直す**（`claude --continue` で会話の続きから戻る）。詳しくは `docs/dev-log/day-3.md` |
| main への push を止める git のフック | リポジトリの git 設定 | `git config core.hooksPath .githooks`（clone し直したとき） |
| GitHub 側の main の保護 | リポジトリの設定（Branches） | 直接 push の禁止・管理者にも適用・承認は不要・強制 push と削除の禁止 |

### 気をつけること
- リポジトリは公開。認証情報（`client_secret.json` / `token.json`）と音声は git の管理外だが、コミットする前に確かめる
- **`Closes #N` をバッククォートで囲むと GitHub が読まない。** 囲まずに書くか、マージした後に手で閉じる（day-3 で2回やった）
- **`ep*/00_logs/` には相談チャットの記録（`chat.json`）が入っている。** ログの掃除のつもりでフォルダごと消すと会話が消える
- **`ep*/00_logs/<工程>.log` は、その工程を動かすたびに上書きされる。** 失敗の様子を残したいときは、動かし直す前に控える
- **未コミットの変更を、確かめずに捨てない。** day-4 に `git reset --hard` で**ユーザーが GUI で保存した設定を消した**。
  追跡ファイルの作業中の変更は reflog にも残らない。いまは `.claude/hooks/block-hard-reset.sh` が止めるが、
  **道具より先に `git status` を見る**
- **状態は「覚えているつもり」で報告しない。** 上の事故のあと「そのまま残してあります」と何度も書いた。
  手元がどうなっているかは、**書く前に `git status` で見る**
- **レビューの指摘や記録は、コードか実際の動きで確かめてから採用する。** day-3 で前提そのものが無い指摘が2件。day-4 で **#85 に残っていた「読み込み15秒」が、試験の作り方の問題**だと分かった
- **切り分ける前に原因を書かない。** day-4 で「markers を付けるとトラックが掴めなくなる」と書きかけたが、
  marker とは無関係で**尺の端で止まっていただけ**だった。marker 無しのものと並べて比べたら分かった
- **逆の結果が出たら、まず測り方を疑う。** 「6本で15秒、25本で2.7秒」という逆転が出て、条件を1つずつ外したら**同じファイルの使い回し**に行き着いた
- **名前を信じない。** `canplay` は「再生できる」であって「波形が描けた」ではない。復号前に `zoom()` を呼ぶと落ちる
- **結論は、測る前と後で変わる。** 案E'（鳴るが見えない）が有利に見えたが、marker の当たり判定を広げられると分かって五分に戻った
- **新しくできるようにした操作には、既存の操作が持っている守りを付け忘れやすい**（#58 で、端を掴んで縮めるときだけ最小長の確認が抜けていた）
- **対称なはずのものは、両方向を試す**（#58 で「音は1つだけ鳴る」を片方向しか確かめていなかった）
- **文書に数字を書くときも「動かしていないものは未確認と書く」**（#58 で「10分で数秒かかる」と断定し、測ったら 1.7秒だった）
- **「安全にしてある」と書いたら、それを守るテストまで書く**（#63。#58 と合わせて2回やった）
- **外部のライブラリやコマンドの振る舞いは、読んで決めつけず測る**（#63 の `acrossfade`、day-4 の multitrack）
- **`git add -A` を確かめずに使わない。** 文書だけの PR ではファイル名で指定する。使ったら `git diff --cached --name-only` を見てからコミットする（#78 で、ユーザーの作業を混ぜた）
- **「確かめたつもり」に気をつける。** WSL の中で `curl` が 200 を返しても、Windows 側から開ける確認にはならない（#75 → #77）
- **Issue を書く前に実機で測る。** #82・#85・#86 とも、動かしたら論点が消えたり増えたりした
- **「どう作るか」より先に「何を作るか」。** 番組の形が決まる前に設計を詰めると、論点の前提が浮く
- **Issue が重くなったら切り出す。** #86 が長くなったので倍率の話を #97 に、さらに #98 / #99 に分けた
- **一時領域（`/tmp/claude-1000/`）は消えることがある。** #86 / #97 の試験用の音声（1.7GB）もここにある。消えたら `ffmpeg` で作り直す
- **文書に「〜だから使わない」と書くときは、その理由が明日も成り立つかを見る**（#70 で、自分で立てた Issue が直った瞬間に嘘になる理由を書いた）
- **「失敗しても進む」は、進んでよい場面かどうかを1つずつ見る**（#70 で、未保存を数えられないときに「無い」ことにして閉じさせていた）
- **番組側（r-hoso）の出力は、すべて GitHub から来る。** メッセージの返信ではなく、
  **Issue へのコメント**や**新しい Issue**として現れる。day-4 に同じ失敗を2回した
  （返事を3回「まだ来ていない」と書いた / 番組側が立てた Issue を3時間半見落とし、件数も間違えた）。
  **r-hoso と何かやりとりしたら、Issue 一覧とコメントを見直す。** `gh issue list` と `gh issue view <番号> --json comments`
- **守りを作ったら、行きすぎも試す。** #107 の hook の最初の版は、文字列として書いただけの
  自分のコミットを止めた。**「止めるべきものを止める」と「通すべきものを通す」の両方を試す**
- **守りは「分からないとき、どちらに倒れるか」で決まる。** #110 で見つかった穴は2つとも
  **判断がつかないときに通す**作りだった。止める側の道具は、**迷ったら止める**方に倒す
- **「確かめた」と言う前に、何を測ったかを確かめる。** day-4 に3回やった
  （消した変更を「残してある」と書いた / Issue 一覧を見直さず件数を間違えた /
  **別のブランチの古い版を測って「レビューが間違っている」と判断した**）。
  **手順ではなくテストにする。** `tests/test_hooks.py` がその形
- **grep の範囲を絞ったまま「全部見た」と書かない**（#111 で `docs/design-prompt.md` を取りこぼした）
- **`gh` が返す時刻は UTC。手元は日本時間で +9時間。** 引き算する前に揃える
  （「半日見落とした」と書いたが実際は約3時間40分だった）
- **「届いていない」と決めつける前に確かめる。** 相手のセッションの記録（`~/.claude/projects/<パス>/*.jsonl`）を
  `cross-session-message` で検索すれば、届いたかどうかが分かる。画面は `herdr pane read <pane>` でも読めるが、
  **履歴が数十行しか残らない**ので当てにしない
- Herdr の担当の名前は、Herdr 全体で重ならないようにする（フリーライフは `director` など、okuradi は `lead` と `impl-<番号>`）
- `herdr worktree remove` は、`--workspace` を省くと今いる場所が対象になる。必ず ID を指定する
