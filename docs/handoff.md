# 申し送り

作業を区切るときに、次の人（次のセッションの Claude）へ残すメモ。**最新の1回分だけを書き、区切るたびに上書きする。** 経緯は `docs/dev-log/` を見る。

**main の位置に、コミットの番号や PR 番号を書かない。** この申し送りはその PR の中で書くので、
番号を書くとマージした瞬間に1つ古くなる。直してもその直しがまた main を進めるので、永久に追いつかない。
位置が要るときは `git log --oneline -1` を打つ。ここに書くのは**番号が変わっても正しいこと**だけにする。

## 2026-09-24（day-7 の区切り）

### いまの状態
- main は、**この申し送りを載せた PR をマージした所**（位置は `git log --oneline -1`）。**開いている PR は無し。ブランチは `main` だけ**
- **`pytest` は 383件。** 数字は写さず `docs/dev-log/day-stats.sh` で数え直す
- **手元に未コミットの変更は無い**
- **番組のリポジトリは別。** [r-hoso](https://github.com/Bohemian1506/r-hoso)（非公開、`~/workspace/r-hoso`）
- **hook が5つ**（`main` への push を止める / `git reset --hard` を止める / 始まるときの状態出し /
  マージしたら議事録を知らせる / **起動したら `/始め` を促す**）
- 波形は wavesurfer.js 7.8.6。**`wavesurfer-multitrack` を採ると決めたが、まだ置いていない**（#87・置くのは #85 のとき）
- 回のデータは `ep01` だけ。**`config.yml`（人が書く）と `timeline.yml`（画面が書く）の2つ**を git で管理する

### 番組の日程が決まった（いちばん大事）

**9/24 の本収録は取りやめになった。** 番組側が体勢を立て直すため（#53 のコメント・2026-09-24）。

| | |
|---|---|
| **第0回（テスト収録）** | **10月頭。** 日付は決まっていない。**中身は、これまで ep01 として作っていた構成・台本のまま** |
| **本収録** | **10月中。** 日付は決まっていない |

- **第0回は公開しない。ただし、公開する体で最後まで一気通貫でやる**（概要欄・クレジットを含む）
- **通しリハーサル（#53）は、第0回で通す**
- r-hoso 側は **`ep00`**（`01_構成/ep00.md`）。**okuradi 側のフォルダ名は決まっていない**
  （番組側から「アプリ側で決めてください」と投げられている）
- **#85 を実装してから通す。** #85 の本文の「#53 を先に片付ける」は**もう有効でない**
  （コメントに注記済み。**本文の古い記述はこれで3つめ**）

### この日に作ったもの

| | 中身 |
|---|---|
| **`session-start-begin.py`**（#181） | **起動したら `/始め` を促す。** 続きから・要約・クリア、知らない値では黙る |
| **許可リスト**（#185） | **`gh pr merge` を許可した。** 運用は「**ユーザーが OK と言ったらマージする**」。仕組みでは止めない |
| **`remind-devlog.sh` の直し**（#189） | **stdout の JSON で Claude に届ける。** stderr に出していたので、day-6 から**2日間一度も届いていなかった** |

### 次にやること

**1発目は #183。ユーザーが決めた**（2026-09-24 の夜）。

1. **#183（画面のペイン）**
   - r-hoso 側と同じように画面を割る。**`/始め` で作り、`/作業終了` で閉じる**（最後の `/exit` は人が打つ）
   - 置くのは3つ: **変更の差分**（**最初は空。直した1回目で映る**）・**GUI のログ**・**今日の一手**
   - **`/作業終了` は「区切り漏れを止める」**。区切っていなければ聞いてから区切る
   - **ペインには名前を付けて、名前で探す**（ID は毎回変わる）
   - **この中で「続きから開いた朝」の穴も塞がる**（下の「まだ確かめていないこと」）
2. **#85 の実装**（GUI をタイムライン編集に切り替える）
   - **最初の一手は `vendor/` に `wavesurfer-multitrack` を置くこと。** まだ置いていない。
     版・ライセンス・落とし穴は `web/static/vendor/README.md`
   - **#153（エコーを生音基準に）は、この中で一緒にやる**
   - **#99 #98（倍率）も、画面ができてから。** 単独では作れない
3. **#182**（ラベルの付け方を決める）

### 開いている Issue（17件・数え直した）
内訳: 段取り 10 / enhancement 3 / v1 2 / **ラベル無し 2**

| Issue | いま |
|---|---|
| **#183** | **画面のペイン。次の一手** |
| **#182** | ラベルの付け方。**ラベル無しの2件は、どちらもこちらが立てたもの**（番組側ではない） |
| #53 `v1` | **第0回（10月頭）で通す。** 手順は `docs/rehearsal.md` |
| #142 `v1` | 単独では直さない（`timeline.yml` でまとめて消す）。踏むのは **ep03** |
| #153 | **決めたことと実装が食い違っている。** #85 と一緒に |
| #79 #82 #85 #88〜#93 | 30分番組化の設計。**地図は #85** |
| #138 | 実測済み（手で入れると操作21回）。**並行作業の練習台** |

### まだ確かめていないこと
- **`/作業終了` の印で、続きから（`start.sh -c`）開いた朝に `/始め` が促されるか**（#183）。
  hook に印を渡すテストはあるが、**実際に終えて続きから開いたことはない**
- **`start.sh` の直しが、実際の再起動で効くか**（再現には再起動が要る）
- **30分・複数コーナーの回を通しで作れるか（#53）**
- **`docs/rehearsal.md` を、人が読みながら通せるか**（機械で画面を操作しただけ）
- **Herdr + `git worktree` の並行作業**（一度もやっていない）
- **タイムラインの読み込みを、倍率を上げた状態・実際の収録音で測っていない**
- **繋いだ音を聴いていない**（長さは合っているが、継ぎ目の音は未確認）
- **ジングル・SE・BGM の形式**（実物がまだ無い）
- **`_why_stale` を更新日時だけに置き換えられるか**
- **30分・複数コーナーの回で、章が実際に並ぶか**
- **リモートにマージ済みのブランチが残っている。** 掃除するかは決めていない

**解けたもの（day-7）**: 「作った hook が知らせを出すか」→ **#189 で直して、届くことを確かめた。**
ただし**届くのは Claude であって、画面には出ない**。

### 決めていないこと（急がない）
- **第0回を okuradi のどのフォルダに置くか**（`ep00` にするか。**番組側から投げられている**）
- ~~`番組側の判断待ち` を使うか / #127 のラベル / #182 の決め方~~ → **#182 で決めた**（2026-09-25。`CLAUDE.md` のラベルの行）
- **合成音声をいつ作るか（収録の前か後か）。** 番組側が「まだ決めていません」と書いている
- **編集長エージェントを置くか**

### 番組側が決めたこと（day-6 の夜・**#175 で実装済み**）

**章の見出しの言葉**（r-hoso #52・案c）。正本は r-hoso の `00_番組仕様/番組の仕様.md`。

| コーナー | 見出し |
|---|---|
| OP | **OP** |
| 今さら聞けない | **今さら聞けない ＋ その回のテーマ** |
| ランテック通信 | **ランテック通信 ＋ その回の話題** |
| 今月のパワープレイ | **今月のパワープレイ ＋ 曲名** |
| ざっくりITニュースヘッドライン | **ざっくりITニュースヘッドライン ＋ 扱ったニュース** |
| 告知 | **お知らせ** ← **コーナー名と違う** |
| ED | **ED** |

- **テーマを付けるのは中身のある4つだけ**
- **「告知」は目次では「お知らせ」。** 台本の定型句が「ここからはお知らせです」で、**耳で聞いた言葉に合わせた**。
  **コーナー名（作り手の言葉）は「告知」のまま**
- **実装済み**（#175）。`series_rules` の `chapter_label` と `chapter_theme` で持つ

### 番組側（r-hoso）とのやりとり
- 向き: **番組の決定がアプリの作りを決める。逆ではない。** 番組側で決めたことは okuradi の Issue に書きに来る
- **番組側は「どう作るかはアプリ側の判断」と毎回書いてくる。** そこはこちらで決めて、**決めたら Issue に書く**
- **同じ日に訂正が来ることがある。** #106 は「章は8個」の数時間後に「8個は間違い」と訂正された。
  **届いた直後に実装しない方がよい場面がある**
- **返事は Issue のコメントか、新しい Issue として来る。** メッセージの返信ではない
- r-hoso は**別の `CLAUDE.md`** を読む。okuradi のセッションから r-hoso を直接いじらない

### 作業の始め方
- GUI の開き方は下の「GUI の起動」（**ここに書くと2か所になって片方が古くなる**）
- コマンド: `.venv/bin/python build.py ep01 --from clean --to clean`（GUI が無くても番組を作れる状態は保つ）
- 追加インストールは `uv pip install --python .venv/bin/python <名前>`（この `.venv` は uv が作ったもので、pip が入っていない）
- WezTerm の F12 から開く（使い方は `docs/herdr.md`）
  - 「herdr: okuradi」: okuradi だけ
  - 「herdr: フリーライフ資料」: okuradi の `lead` も起動してから、フリーライフを開く
  - 「herdr: okuradi + フリーライフ」: 両方を起動して、okuradi を開く
  - 「herdr: okuradi + アール放送局」: 両方の画面を用意して、okuradi を開く（アール放送局は画面だけ）
  - 「herdr: 全部（フリーライフ + okuradi + アール放送局）」: 3つとも
- 前の会話の続きから始めるときは、端末で `~/workspace/okuradi/start.sh -c`
  - **`/作業終了` で終えた日は、続きから開いても `/始め` が促される**（印を見る。#183）。
    **`/作業終了` を通さずに閉じた日は、`/始め` を自分で打つ**
    （`-c` は `source` が `resume` になり、印が無いと促しが出ない）
- 作業のルールは `CLAUDE.md`（Issue から始める / 勝手に決めない / レビュー担当は必要なときに呼ぶ / マージしたらすぐ議事録の PR）

### GUI の起動
```
.venv/bin/python -m uvicorn web.main:app --host 0.0.0.0 --reload   # 起動
./open-gui.sh                                                       # ブラウザで開く
```
- **作業のペインがあるときは `.claude/scripts/作業ペイン.sh gui`**（GUIログのペインの中で上の1行目を打つ。#183）
- **`--host 0.0.0.0` が要る。** 付けないと WSL の中だけで待ち、Windows 側のブラウザから入れない
- **`http://localhost:8000` は通らない**（Windows 10 では WSL の localhost 転送が効かない。#77 で確かめた）
- `./open-gui.sh` が、そのときの WSL の IP を調べてブラウザを開く。**IP は再起動すると変わる**ので、数字を文書に書かないこと
- サーバーが動いていない / `127.0.0.1` だけで待っているときは、`open-gui.sh` が開かずに理由を出して止まる
- `--host 0.0.0.0` は同じ LAN の他の機械からも見える。外のネットワークでは避ける

### 画面の確かめ方（CDP）
ヘッドレスの Chrome を CDP で動かすと、**実際にマウスを動かしてドラッグまで試せる**。#86 / #97 はこれで測った。

- ブラウザは `~/.cache/ms-playwright/chromium-*/chrome-linux*/chrome`。**版は勝手に入れ替わる**ので `ls` で探す
- 起動: `--headless=new --remote-debugging-port=9222 --no-sandbox --user-data-dir=<作業場所>`
- **`pkill -f "remote-debugging-port=9222"` を使わない。** パターンが自分のコマンド行にも当たってシェルごと死ぬ。`ps` で PID を拾って `kill` する
- 測るときは `python3 -m http.server` で配る。**キャッシュが効くので、冷えた状態を測るなら URL に `?v=<毎回違う値>` を付ける**

### リポジトリの外にある設定（消えたら作り直す）

| 設定 | 場所 | 作り直し方 |
|---|---|---|
| `.wslconfig`（Windows 側） | `C:\Users\hiros\.wslconfig` に `networkingMode=mirrored` と書いてあるが、**Windows 10 では効かない**（mirrored は Windows 11 22H2 以降）。消すか注記するかは #77 で決める |
| F12 のメニュー（**5つ**） | `C:\Users\hiros\.wezterm.lua` の `launch_menu` | 書き方は `docs/dev-log/day-2.md` の #13 と `docs/herdr.md` |
| Herdr の Claude 連携 | `~/.claude/hooks/herdr-agent-state.sh` など | `herdr integration install claude` |
| Claude が Herdr を操作するスキル | `~/.claude/skills/herdr/SKILL.md` | `herdr --skill > ~/.claude/skills/herdr/SKILL.md`（Herdr を更新したときも） |
| Claude Design のプロジェクト | `共通の枠を出しました`（claude.ai/design）。**見本はここが正本**。実装が見本を追い越したら、`github.md` の同期メモと一緒に直す（#65・#70 でやった）。**直したら `docs/design/` にも取り込み直す**（`serve_url` に `&raw=1` を付けると元のファイルが取れる）。どちらの見本を見るかは `docs/design/README.md` の表 |
| Claude Design とつなぐ MCP | `~/.claude.json`（`-s user` で入れた。公開リポジトリなので `.mcp.json` は作らない） | `claude mcp add -s user --transport http claude_design https://api.anthropic.com/v1/design/mcp` → `/design-login` → **Claude を立ち上げ直す**（`claude --continue` で会話の続きから戻る）。詳しくは `docs/dev-log/day-3.md` |
| main への push を止める git のフック | リポジトリの git 設定 | `git config core.hooksPath .githooks`（clone し直したとき） |
| GitHub 側の main の保護 | リポジトリの設定（Branches） | 直接 push の禁止・管理者にも適用・承認は不要・強制 push と削除の禁止 |

### 気をつけること
- リポジトリは公開。認証情報（`client_secret.json` / `token.json`）と音声は git の管理外だが、コミットする前に確かめる。**いまは `tests/test_guards.py` が守っている**
- **`Closes #N` をバッククォートで囲むと GitHub が読まない。** 囲まずに書くか、マージした後に手で閉じる（day-3 で2回やった）
- **`ep*/00_logs/` には相談チャットの記録（`chat.json`）が入っている。** ログの掃除のつもりでフォルダごと消すと会話が消える
- **`ep*/00_logs/<工程>.log` は、その工程を動かすたびに上書きされる。** 失敗の様子を残したいときは、動かし直す前に控える
- **未コミットの変更を、確かめずに捨てない。** day-4 に `git reset --hard` で**ユーザーが GUI で保存した設定を消した**。
  いまは `.claude/hooks/block-hard-reset.sh` が止めるが、**道具より先に `git status` を見る**。
  day-5 は**捨てる前に中身を見せて聞いた**（ユーザーの判断は「捨てる」）
- **状態は「覚えているつもり」で報告しない。** 手元がどうなっているかは、**書く前に `git status` で見る**
- **レビューの指摘や記録は、コードか実際の動きで確かめてから採用する。**
  day-5 の #129 で、🚨 の**半分が「この PR のせいではない」**と分かった。そのまま直していたら関係ない所を変えていた
- **測る場所を間違えない。** day-5 に「申し送りの 232件は古い、実測 235件」と書いたが、
  **235件は別ブランチの数**で main では 232件が正しかった。day-4 の「別のブランチの古い版を測った」と**同じ形で2回目**
- **grep の範囲とパターンを確かめてから「無い」と書く。** day-5 に「`code-reviewer.md` に裏取りの要求は無い」と書いたが、
  **全角コロンにパターンが当たっていなかっただけ**で、実際にはあった。day-4 の「範囲を絞った grep」と**同じ形で2回目**
- **切り分ける前に原因を書かない。** day-4 で「markers を付けるとトラックが掴めなくなる」と書きかけたが、
  marker とは無関係で**尺の端で止まっていただけ**だった
- **逆の結果が出たら、まず測り方を疑う。** 「6本で15秒、25本で2.7秒」という逆転は**同じファイルの使い回し**だった
- **名前を信じない。** `canplay` は「再生できる」であって「波形が描けた」ではない
- **新しくできるようにした操作には、既存の操作が持っている守りを付け忘れやすい**（#58）
- **対称なはずのものは、両方向を試す**（#58）
- **文書に数字を書くときも「動かしていないものは未確認と書く」**（#58 で「10分で数秒かかる」と断定し、測ったら 1.7秒だった）
- **「安全にしてある」と書いたら、それを守るテストまで書く**（#63。day-5 の #130 がその3回目になりかけていた）
- **外部のライブラリやコマンドの振る舞いは、読んで決めつけず測る**（#63 の `acrossfade`、day-4 の multitrack）
- **`git add -A` を確かめずに使わない。** 文書だけの PR ではファイル名で指定する（#78 で、ユーザーの作業を混ぜた）
- **「確かめたつもり」に気をつける。** WSL の中で `curl` が 200 を返しても、Windows 側から開ける確認にはならない（#75 → #77）
- **Issue を書く前に実機で測る。** #82・#85・#86 とも、動かしたら論点が消えたり増えたりした
- **「どう作るか」より先に「何を作るか」。** 番組の形が決まる前に設計を詰めると、論点の前提が浮く
- **Issue が重くなったら切り出す。** #86 が長くなったので倍率の話を #97 に、さらに #98 / #99 に分けた
- **一時領域（`/tmp/claude-1000/`）は消えることがある。** #86 / #97 の試験用の音声（1.7GB）もここにある
- **文書に「〜だから使わない」と書くときは、その理由が明日も成り立つかを見る**（#70）
- **「失敗しても進む」は、進んでよい場面かどうかを1つずつ見る**（#70）
- **番組側（r-hoso）の出力は、すべて GitHub から来る。** **r-hoso と何かやりとりしたら、Issue 一覧とコメントを見直す**
- **守りを作ったら、行きすぎも試す。** #107 の hook の最初の版は、文字列として書いただけの自分のコミットを止めた。
  **day-5 の C の試作は逆で、狙った語を取り逃した。** 両方向を試す
- **守りは「分からないとき、どちらに倒れるか」で決まる。** 止める側の道具は、**迷ったら止める**方に倒す。
  ただし**担当（サブエージェント）が裁く形にすれば、機械の側は「迷ったら出す」に倒せる**（C の設計）
- **「確かめた」と言う前に、何を測ったかを確かめる。** **手順ではなくテストにする。** `tests/test_hooks.py` と `tests/test_guards.py` がその形
- **`gh` が返す時刻は UTC。手元は日本時間で +9時間。** 引き算する前に揃える
- **Issue を起票するときに「直す手順」まで書いておく。** #112 でそのとおり進んで迷わなかった。**自分への申し送りになる**
- **数える前に決めない。** 許可リストのとき `fuser` を読み取り専用だと思い込んでいたが、**実物は全部 `-k`** だった
- **毎回走るものは、壊れ方を先に決める。** 始まるときの hook は `gh` が固まると 45秒かかっていた
- **「届いていない」と決めつける前に確かめる。** 相手のセッションの記録（`~/.claude/projects/<パス>/*.jsonl`）を検索すれば分かる
- Herdr の担当の名前は、Herdr 全体で重ならないようにする（フリーライフは `director` など、okuradi は `lead` と `impl-<番号>`）
- `herdr worktree remove` は、`--workspace` を省くと今いる場所が対象になる。必ず ID を指定する
- **同じ作業フォルダで Claude を2つ動かさない。** 分けるなら `git worktree`。作業に入る前に `git branch --show-current` を見る
- **件数は数え直す。引き算で出さない。** day-4 と day-5 で2回間違えた
- **画面の説明を書くときは、実物のボタン名を見る。** day-5 に「確定を押す」と書いたが、実物は「確認した」だった。
  同じ日の夜、#129 では「＋目次1件」を `app.js:523` で確かめてから書いた
- **本文だけ読んで「未解決」と言わない。** day-6 に「数十レーン問題は未解決」と言ったが、**#86 のコメントで決着していた**。
  **ユーザーの記憶のほうが正しかった。** 冒頭のサマリは更新漏れで古い（`CLAUDE.md`「末尾や本体を見る」）
- **もっともらしい数字ほど疑う。** day-6 に、**ライブラリが 404 のまま**読み込みを測って「canplay 0.3秒」を得た。
  **波形が1枚も描けていない**のがおかしいと思って初めて気づいた
- **`canplay` は「再生できる」であって「波形が描けた」ではない**（day-6 で実証。0.2秒 対 4秒）。
  **wavesurfer は shadow DOM の中に描くので `document.querySelectorAll('canvas')` では見つからない**
- **`pkill -f` を使わない。** day-6 に `pkill -f "http.server 8777"` を打って、**自分のコマンド行に当たりシェルが落ちた。**
  **申し送りに書いてあるのに使った。** `ps` で PID を拾って `kill` する
- **「いつ踏むか」を言う前に、ひな型をたどる。** day-6 に #142 を「9/24 に踏む」と書いたが、
  **ひな型の `ep01` にエコー区間が無い**ので踏まない。踏むのは `ep03`
- **外部コマンドが成功しても、結果が正しいとは限らない。** day-6 に、24kHz と 48kHz の wav を `-f concat` で繋いだら
  **エラーを出さずに倍速になった**（10秒が5秒）。`filter_complex` でレートをそろえれば `duration_ts` まで一致する
- **「ついでの改修はしない」の範囲。** プラスアルファの機能や別件の修正はやらない。
  **同じ関数の同じ欠陥は、最後まで直す**（half だけ直すほうが悪い）。day-6 にユーザーが明示した
- **時刻を推測で言わない。** day-6 に一度測ったあと頭で引き算し、**4回続けて間違えた**（30分ずれた）。
  `date` を打つ。**数字は測り直すという決まりは、時刻にも効く**
- **ブランチを切る前に `git branch --show-current` を見る。** day-6 に `main` に戻らずに切って、
  **議事録の PR にコードが混ざり、そのマージで別の PR の中身が main に入った**
- **決定は Issue と正本の両方に書く。** day-6 に「Issue には書いた、正本は直していない」を**3回**やった。
  **Issue は決めた経緯、`docs/` は決まった結果**で、役割が違う
- **2つあるものは、必ず両方見る。** day-6 に `positions` と `total_seconds` が別々に足していて
  **0.0013秒 食い違っていた**。同じ答えを2か所で出さない
- **`glob("*.wav")` は `second.WAV` を拾わない**（Linux は大文字小文字を区別する）。
  拡張子で拾うときは `suffix.lower()` を使う
- **担当（サブエージェント）は私と同じフォルダで動く。** 読むだけなら安全。
  **実装を任せるならブランチが衝突する**ので、`git worktree` で分ける
- **`test-writer` を呼ぶ。** day-6 に呼び忘れ、ユーザーに聞かれて気づいた。
  呼んだら**実装の穴が2つ**出た（うち大きいほうは「確かめられなかったこと」から見つかった）
- **「止めるか、知らせるか」は「それが正しい場面があるか」で決める。** あるなら止めない。
  **止めないときは「このままで大丈夫です」と添える**（無いと不具合だと思わせる）
- **判定が2か所にあることがある**（サーバーと画面）。**片方だけ直すとずれる。** そろっていることをテストで固定する
- **こちらから r-hoso に Issue を立てることがある。** 返事は向こうに来る。
  **`/始め` と `/区切り` で r-hoso も見る**（#172 で足した）
- **仮の文を入れたら、番組側に返す。** day-6 に `theme_hint` を仮で書いたら、**3つとも言い回しを直された**。
  とくに「この回の挨拶」は**半分が毎回同じ内容**で、テーマ欄に書くことではなかった
- **テストを書き換えるときは「期待値を緩めただけ」になっていないか見る。** day-6 に、
  緩めた1件のうしろに**並びの崩れが隠れていた**（レビューで見つかった）
- **作る前に測る。** day-6 に #132 の機械を測ったら、**狙った失敗に当たっていなかった**
- **`git show` はマージコミットの差分を既定で出さない。** `-m --first-parent` が要る
- **「消した値」と「消した文」は別物。** 取りこぼしは**文**のほうで起きる
- **`ep01` は、番組の ep01 であると同時に、いまは試験用の入れ物でもある。**
  `config.yml` の `segments` が1つなのは**テスト収録のときの値**で、**本番は7コーナー**（9/24）。
  **混ぜて読み、day-6 に2回間違えた**。**回の本番の姿は `r-hoso/01_構成/` にある**
- **同じ置換を何度も重ねない。** day-6 に、すでに消した節をまた消そうとして**3回空振り**した。
  **失敗したら、実物を `grep` で見てから書き直す**
- **ユーザーの案の方が良いことがある。** day-5 の C の設計は、こちらの案より**ユーザーの「騒音を裁く係を挟む」の方が良かった**。
  自分の案を守らずに、良い方を採る
- **知らせる系の hook は、出し方を間違えると静かに消える。** day-6 に作った議事録の知らせは
  **`stderr` に出していた**ので、終了コード 0 では**どこにも表示されず、2日間ずっと届いていなかった**（#189）。
  **止める系は止まらなければ気づくが、知らせる系は気づけない**
- **テストが緑でも、届いているとは限らない。** その hook のテストは `proc.stderr` を見ていたので通っていた。
  **「作った」と「届いた」は別**
- **`gh issue view <番号>` は PR も表示する。** day-7 に「hook 系の Issue はラベル無しだ」と報告したが、
  根拠の3件は**全部 PR** だった。**Issue かどうかは `state` で分かる**（`MERGED` なら PR）
- **設定はセッションの途中でも反映される**（day-7 に確かめた）。「効くのは次に開いてから」と説明したのは**間違いだった**
- **Claude は、自分の権限を広げる変更を自分でコミットできない**（day-7 に2度止められた。**読み取りまで止まる**）。
  **設定を変えるときはユーザーが打つ。** これは妥当な守りなので、回避しない
- **消した値ではなく、いま書いている言葉で探す。** `grep -rn "<いま直している言葉>"`。
  取りこぼしは「消した値」ではなく「**書き忘れた文**」で起きる（#132 で測った）
