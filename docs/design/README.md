# デザインの見本

[Claude Design のプロジェクト「共通の枠を出しました」](https://claude.ai/design/p/799a3b17-c9a3-402e-ac05-e0b58a63c4d2)から取り込んだ、画面の見本。
指示文は `docs/design-prompt.md`、部品の仕様は `docs/components.md`。

取り込み日: 2026-09-19（#4 で取り込み、#72 で追加ぶんを取り込み直した）

## ここにあるもの

| ファイル | 中身 |
|---|---|
| `frame-common-v2.dc.html` | 共通の枠（部品1〜7）。状態違いも並んでいる |
| `screen1-episode-settings.dc.html` | 画面1 回の設定（部品8） |
| `screen2-recording-cleanup.dc.html` | 画面2 収録〜整音（部品9〜15） |
| `screen3-finishing.dc.html` | 画面3 仕上げ（部品16〜19、編集できる文字起こし） |
| `support.js` | Claude Design が `.dc.html` を描くための部品。無いと見本が表示できない |
| `部品13・15・19 の見直し.dc.html` | **見本と実装のずれの表**と、#64（動画の再生ボタン）・#65（帯の見分け）の案。実装が先に進んだぶんはここで追う |
| `部品13の読み込み中.dc.html` | 部品13「読み込み中」の**正の見本**（#70）。下の「どちらを見るか」を参照 |

## どちらを見るか（見本が2つある部品）

実装のほうが先に進んで、`screen1/2/3` が追いついていない部品がある。**下の表が優先**。

| 部品 | 見る見本 | screen2/3 に描いてあるもの |
|---|---|---|
| 13 読み込み中 | `部品13の読み込み中.dc.html` | 中央にくるくる（**採用しない**。#70） |
| 13・15 エコーの帯 | `部品13・15・19 の見直し.dc.html` | 帯が無い |
| 19 動画の再生ボタン | `部品13・15・19 の見直し.dc.html` | 中央に大きなボタン（#64 でこちらを採用した） |

そのほか、`screen1/2/3` に入っていない実装は `部品13・15・19 の見直し.dc.html` の枠1 にまとめてある
（波形が wavesurfer.js になった、プレーヤーが音ごとに4つになった、など）。

取り込まなかったもの:
- 雰囲気の案A（深夜放送）・案C（キューシート）。**案B トランジスタに決まった**ため
- 共通の枠 v1。v2 に置き換わったため
- 案Bのテーマ見本そのもの。色と字は下の表に写してあるので、見本は画面3つで足りる

## これは「見本」であって、そのままの画面ではない

実装（#5〜#8）に入る前に知っておくこと。

- **`class=` が0個、`style=` が615個**。見た目はすべて要素に直接書かれている。実装では、ここから CSS を起こす
- `<x-dc>` という Claude Design 専用のタグで包まれていて、描くのに `support.js` が要る。配信用の HTML としては使えない
- そのまま使えるのは、**色・字・余白・部品の形・画面の文言**

## 決まった雰囲気（案B トランジスタ — クリームとウォルナット）

60年代の卓上ラジオ。工程はチューニング目盛りで、針が今いる工程を指す。丸い角と太い見出し。

### 字
| 用途 | 書体 |
|---|---|
| 本文・見出し | Zen Kaku Gothic New（400 / 500 / 700 / 900） |
| 数字・時刻・ファイル名 | DM Mono（400 / 500） |

どちらも Google Fonts。見出しは 900 まで使う。

### 色
| 役割 | 値 |
|---|---|
| 文字・濃い面（サイドバー、波形の背景） | `#3a2418` |
| いちばん明るい面（カード、ヘッダー、フッター） | `#fffaf0` |
| 下地 | `#f6efe0` |
| 濃い面の上の文字 | `#fff3dc` |
| 薄い文字・補足 | `#7a6250` |
| 押せない文字 | `#a08c78` |
| 罫線（薄い） | `#e3d7bf` |
| 罫線（濃い） | `#cdbfa5` |
| 主役の色（実行中・ボタン・強調） | `oklch(0.65 0.18 45)` |
| 完了 | `oklch(0.6 0.13 150)` |
| エラー | `oklch(0.55 0.2 25)` |

### 形
- ボタン・タブ・チップは `border-radius:999px`（丸い）
- カードは `10px` 〜 `14px`
- 画面の幅は 1120px を基準に描かれている（デスクトップのみ）

## 直すとき

Claude Design 側を直してから、こちらに取り込み直す（こちらを手で直さない）。

1. Web 版の Claude Design でプロジェクトを開いて直す
2. Claude Code で `claude_design` の MCP から `read_file` で読み、`docs/design/` に上書きする
   - MCP の入れ方は `docs/handoff.md` の「リポジトリの外にある設定」
